# performance-speculation-view-transitions

> Generated by the **Writer** (Reversa) · unit granularity `module` · doc_level `complete`
> Source: WordPress `7.2-alpha-63330` · extracted by the Archaeologist from the files listed below.
> Confidence on every statement: 🟢 CONFIRMED (read from code) · 🟡 INFERRED · 🔴 GAP

---

## Overview

Modern browser-performance primitives: the Speculation Rules API for prefetch and prerender, plus View Transitions. 🟢

**Complexity:** `low` · **Business rules extracted:** 6 · **Public functions catalogued:** 6 · **Extension points:** 3

**Depended on by 0 module(s):** _none — terminal module_

## Responsibilities

- **two-axis speculation configuration** — mode (prefetch vs prerender) crossed with eagerness (conservative, moderate, eager), defaulting to the least aggressive pair, with a hosting-provider override channel 🟢

## Data owned

### `speculation rules configuration` 🟢

| Field | Type | Required | Note |
|-------|------|:--------:|------|
| `mode` | `'prefetch'|'prerender'` | yes | prefetch |
| `eagerness` | `'conservative'|'moderate'|'eager'` | yes | conservative |

## Business Rules

Every rule below was read from the source file cited. These are the contract.

| ID | Rule | Source | Confidence |
|----|------|--------|:----------:|
| `BR-PERF-01` | Speculation mode is prefetch or prerender; eagerness is conservative, moderate or eager. | `wp-includes/speculative-loading.php:22` | 🟢 |
| `BR-PERF-02` | Core defaults to prefetch with conservative eagerness, the most cautious combination. | `wp-includes/speculative-loading.php:125` | 🟢 |
| `BR-PERF-03` | Hosting providers may override the default configuration via a documented override path. | `wp-includes/speculative-loading.php:158` | 🟢 |
| `BR-PERF-04` | URL patterns are prefixed for subdirectory installs by WP_URL_Pattern_Prefixer. | `wp-includes/class-wp-url-pattern-prefixer.php` | 🟢 |
| `BR-PERF-05` | Rules are emitted as a speculationrules JSON script block. | `wp-includes/speculative-loading.php:330` | 🟢 |
| `BR-PERF-06` | View Transitions support in this release is limited to admin CSS plus a configuration surface. | `wp-includes/view-transitions.php` | 🟢 |

## Functional Requirements

| ID | Requirement | Priority | Acceptance criterion |
|----|-------------|----------|----------------------|
| FR-01 | Speculation mode is prefetch or prerender; eagerness is conservative, moderate or eager | Must | Verify at `wp-includes/speculative-loading.php:22` |
| FR-02 | Core defaults to prefetch with conservative eagerness, the most cautious combination | Must | Verify at `wp-includes/speculative-loading.php:125` |
| FR-03 | Hosting providers may override the default configuration via a documented override path | Should | Verify at `wp-includes/speculative-loading.php:158` |
| FR-04 | URL patterns are prefixed for subdirectory installs by WP_URL_Pattern_Prefixer | Should | Verify at `wp-includes/class-wp-url-pattern-prefixer.php` |
| FR-05 | Rules are emitted as a speculationrules JSON script block | Should | Verify at `wp-includes/speculative-loading.php:330` |
| FR-06 | View Transitions support in this release is limited to admin CSS plus a configuration surface | Should | Verify at `wp-includes/view-transitions.php` |

## Non-Functional Requirements

| Type | Inferred requirement | Evidence in the code | Confidence |
|------|----------------------|----------------------|------------|
| Scalability | URL patterns are prefixed for subdirectory installs by WP_URL_Pattern_Prefixer | `wp-includes/class-wp-url-pattern-prefixer.php` | 🟢 |

> Inferred from code evidence only. Lines with no traceable evidence were omitted.

## Acceptance Criteria

```gherkin
# BR-PERF-01
Given the performance-speculation-view-transitions module is loaded
When the behaviour at wp-includes/speculative-loading.php:22 is exercised
Then Speculation mode is prefetch or prerender; eagerness is conservative, moderate or eager

# BR-PERF-02
Given the performance-speculation-view-transitions module is loaded
When the behaviour at wp-includes/speculative-loading.php:125 is exercised
Then Core defaults to prefetch with conservative eagerness, the most cautious combination

# BR-PERF-03
Given the performance-speculation-view-transitions module is loaded
When the behaviour at wp-includes/speculative-loading.php:158 is exercised
Then Hosting providers may override the default configuration via a documented override path

# BR-PERF-04
Given the performance-speculation-view-transitions module is loaded
When the behaviour at wp-includes/class-wp-url-pattern-prefixer.php is exercised
Then URL patterns are prefixed for subdirectory installs by WP_URL_Pattern_Prefixer

# BR-PERF-05
Given the performance-speculation-view-transitions module is loaded
When the behaviour at wp-includes/speculative-loading.php:330 is exercised
Then Rules are emitted as a speculationrules JSON script block

# BR-PERF-06
Given the performance-speculation-view-transitions module is loaded
When the behaviour at wp-includes/view-transitions.php is exercised
Then View Transitions support in this release is limited to admin CSS plus a configuration surface

# Failure path
Given an input that violates the constraints above
When the operation is attempted
Then the module must fail in the manner its rules describe
And must not silently produce a partial result
```

## Priority (MoSCoW)

| Requirement | MoSCoW | Rationale |
|-------------|--------|-----------|
| Core responsibilities | Must | terminal module — no other unit depends on it |
| `BR-PERF-03` | Should | Secondary rule; terminal module — no other unit depends on it |
| `BR-PERF-04` | Should | Secondary rule; terminal module — no other unit depends on it |
| `BR-PERF-05` | Should | Secondary rule; terminal module — no other unit depends on it |
| `BR-PERF-06` | Should | Secondary rule; terminal module — no other unit depends on it |

> Priority inferred from dependent count (`traceability/spec-impact-matrix.md`) and position in the dependency chain.

## Code Traceability

| File | Function / Class | Coverage |
|------|------------------|----------|
| `wp-includes/speculative-loading.php` | `wp_get_speculation_rules_configuration`, `wp_get_speculation_rules_default_configuration`, `wp_get_speculative_loading_override`, `wp_get_speculation_rules`, `wp_print_speculation_rules` | 🟢 |
| `wp-includes/class-wp-speculation-rules.php` | _module file_ | 🟡 |
| `wp-includes/class-wp-url-pattern-prefixer.php` | _module file_ | 🟡 |
| `wp-includes/view-transitions.php` | `wp_enqueue_view_transitions_admin_css` | 🟢 |
| `wp-includes/view-config.php` | _module file_ | 🟡 |
| `wp-includes/class-wp-view-config-data.php` | _module file_ | 🟡 |

## Open points

- 🟡 **F-PERF-03** — View Transitions ships infrastructure ahead of the feature: config classes exist before there is much to configure.

See `../questions.md` for questions raised across the whole extraction.

