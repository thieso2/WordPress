# admin-application — Implementation Tasks

> Generated by the **Writer** (Reversa) · doc_level `complete`
> A sequence of executable tasks to reimplement this unit from the legacy system.
> Every task cites the legacy file the behaviour was extracted from.

---

## Prerequisites

- [ ] Unit `authentication-and-sessions` is available (see `design.md` → Dependencies)
- [ ] Unit `users-roles-capabilities` is available (see `design.md` → Dependencies)
- [ ] Unit `bootstrap-and-load` is available (see `design.md` → Dependencies)
- [ ] Unit `cron` is available (see `design.md` → Dependencies)
- [ ] Unit `options-and-transients` is available (see `design.md` → Dependencies)
- [ ] Unit `hooks-plugin-api` is available (see `design.md` → Dependencies)
- [ ] Unit `filesystem-api` is available (see `design.md` → Dependencies)
- [ ] Unit `updates-and-upgrader` is available (see `design.md` → Dependencies)
- [ ] Database schema matches `../data-dictionary.md` for: `WP_List_Table`
- [ ] PHP 7.4+ with the extensions listed in `../dependencies.md` §2

## Tasks

- [ ] **T-01** — Implement the *shared bootstrap gate* algorithm.
  - Behaviour: admin.php defines the admin context constants, loads WordPress, requires the admin includes, and calls auth_redirect() once before firing admin_init, so every screen inherits authentication.
  - Legacy source: `wp-admin/admin.php`
  - Definition of done: the algorithm reproduces the cited behaviour exactly, including its edge conditions
  - Confidence: 🟢

- [ ] **T-02** — Implement the *action-name ajax dispatch* algorithm.
  - Behaviour: $_REQUEST['action'] selects wp_ajax_{action} for logged-in users or wp_ajax_nopriv_{action} for anonymous ones; the nopriv variant IS the authorization model.
  - Legacy source: `wp-admin/admin-ajax.php`
  - Definition of done: the algorithm reproduces the cited behaviour exactly, including its edge conditions
  - Confidence: 🟢

- [ ] **T-03** — Implement `auth_redirect()` returning `void`.
  - Legacy source: `wp-includes/pluggable.php:1280`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-04** — Implement `add_menu_page($page_title, $menu_title, $capability, $menu_slug, $callback = '', $icon_url = '', $position = null)` returning `string`.
  - Legacy source: `wp-admin/includes/plugin.php`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-05** — Implement `add_submenu_page($parent_slug, $page_title, $menu_title, $capability, $menu_slug, $callback = '', $position = null)` returning `string|false`.
  - Legacy source: `wp-admin/includes/plugin.php`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-06** — Enforce BR-ADM-01: WP_ADMIN is defined by admin.php; WP_NETWORK_ADMIN and WP_USER_ADMIN distinguish the three admin contexts.
  - Legacy source: `wp-admin/admin.php:14`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-07** — Enforce BR-ADM-02: auth_redirect() at admin.php line 104 is the single authentication gate for every admin screen.
  - Legacy source: `wp-admin/admin.php:104`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-08** — Enforce BR-ADM-03: admin_init fires after the admin bootstrap and before any screen-specific code.
  - Legacy source: `wp-admin/admin.php:180`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-09** — Enforce BR-ADM-04: wp_scheduled_delete and delete_expired_transients are re-scheduled on admin load if absent.
  - Legacy source: `wp-admin/admin.php:107`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-10** — Enforce BR-ADM-05: A db_upgraded option triggers the upgrade path on admin load.
  - Legacy source: `wp-admin/admin.php:39`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-11** — Enforce BR-ADM-06: admin-ajax.php dispatches on $_REQUEST['action'] to wp_ajax_{action} or wp_ajax_nopriv_{action}.
  - Legacy source: `wp-admin/admin-ajax.php`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-12** — Enforce BR-ADM-07: Registering a wp_ajax_nopriv_* handler makes that action publicly callable with no further gate; each handler must check capabilities and nonces itself.
  - Legacy source: `wp-admin/admin-ajax.php`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-13** — Enforce BR-ADM-08: WP_List_Table subclasses implement prepare_items() and per-column methods.
  - Legacy source: `wp-admin/includes/class-wp-list-table.php`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

## Test Tasks

- [ ] **TT-01** — Happy-path test for the main flow (see `requirements.md` → Acceptance Criteria)
- [ ] **TT-02** — Failure-path test for the main error case
- [ ] **TT-03** — Assert BR-ADM-01 and its negative case
- [ ] **TT-04** — Assert BR-ADM-02 and its negative case
- [ ] **TT-05** — Assert BR-ADM-03 and its negative case
- [ ] **TT-06** — Assert BR-ADM-04 and its negative case
- [ ] **TT-07** — Assert BR-ADM-05 and its negative case
- [ ] **TT-08** — Assert BR-ADM-06 and its negative case
- [ ] **TT-09** — Assert BR-ADM-07 and its negative case
- [ ] **TT-10** — Assert BR-ADM-08 and its negative case
- [ ] **TT-11** — Assert every extension point in `design.md` can alter behaviour as documented

> ⚠️ **No legacy tests exist for this unit.** This checkout contains no test suite (`../inventory.md` §9, question Q1). Every test above must be written from the specification, not ported.

## Data Migration Tasks

- [ ] **TM-01** — Provision `WP_List_Table` per `../data-dictionary.md`
  - ⚠️ No foreign keys exist in the legacy schema; referential integrity is caller-enforced (`F-DD-01`)

## Suggested Order

1. Algorithms first (T-01…) — they encode the behaviour the functions expose.
2. Public functions next, in dependency order within the unit.
3. Business-rule enforcement last, with a test per rule.
4. **Blocker:** this unit cannot be validated until `authentication-and-sessions`, `users-roles-capabilities`, `bootstrap-and-load`, `cron` exist.

> ⚠️ **This unit is inside the 23-module dependency cycle** (`../traceability/spec-impact-matrix.md` §1). It cannot be fully validated in isolation; plan an integration checkpoint with the other members of the cycle.

