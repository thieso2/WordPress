# cron — Implementation Tasks

> Generated by the **Writer** (Reversa) · doc_level `complete`
> A sequence of executable tasks to reimplement this unit from the legacy system.
> Every task cites the legacy file the behaviour was extracted from.

---

## Prerequisites

- [ ] Unit `options-and-transients` is available (see `design.md` → Dependencies)
- [ ] Unit `http-api` is available (see `design.md` → Dependencies)
- [ ] Unit `hooks-plugin-api` is available (see `design.md` → Dependencies)
- [ ] Unit `cache-and-object-cache` is available (see `design.md` → Dependencies)
- [ ] Database schema matches `../data-dictionary.md` for: `cron option`
- [ ] PHP 7.4+ with the extensions listed in `../dependencies.md` §2

## Tasks

- [ ] **T-01** — Implement the *asymmetric duplicate window* algorithm.
  - Behaviour: For events due within 10 minutes or already past, min_timestamp becomes 0 so all past identical events count as duplicates; for far-future events the window is symmetric around the event's own timestamp.
  - Legacy source: `wp-includes/cron.php:135`
  - Definition of done: the algorithm reproduces the cited behaviour exactly, including its edge conditions
  - Confidence: 🟢

- [ ] **T-02** — Implement the *transient-based spawn lock* algorithm.
  - Behaviour: A doing_cron transient holds a microtime float; locks more than 10 minutes ahead are discarded, and WP_CRON_LOCK_TIMEOUT doubles as a one-per-minute rate limit.
  - Legacy source: `wp-includes/cron.php:899`
  - Definition of done: the algorithm reproduces the cited behaviour exactly, including its edge conditions
  - Confidence: 🟢

- [ ] **T-03** — Implement the *visitor-redirect fallback* algorithm.
  - Behaviour: ALTERNATE_WP_CRON redirects the visitor's browser back to the same URI with a marker and runs cron during that second request, trading user latency for reliability on hosts where loopback fails.
  - Legacy source: `wp-includes/cron.php:936`
  - Definition of done: the algorithm reproduces the cited behaviour exactly, including its edge conditions
  - Confidence: 🟢

- [ ] **T-04** — Implement `wp_schedule_single_event($timestamp, $hook, $args = array(), $wp_error = false)` returning `bool|WP_Error`.
  - Legacy source: `wp-includes/cron.php:48`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-05** — Implement `wp_schedule_event($timestamp, $recurrence, $hook, $args = array(), $wp_error = false)` returning `bool|WP_Error`.
  - Legacy source: `wp-includes/cron.php:252`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-06** — Implement `wp_reschedule_event($timestamp, $recurrence, $hook, $args = array(), $wp_error = false)` returning `bool|WP_Error`.
  - Legacy source: `wp-includes/cron.php:367`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-07** — Implement `wp_unschedule_event($timestamp, $hook, $args = array(), $wp_error = false)` returning `bool|WP_Error`.
  - Legacy source: `wp-includes/cron.php:489`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-08** — Implement `wp_clear_scheduled_hook($hook, $args = array(), $wp_error = false)` returning `int|false|WP_Error`.
  - Legacy source: `wp-includes/cron.php:576`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-09** — Implement `wp_next_scheduled($hook, $args = array())` returning `int|false`.
  - Legacy source: `wp-includes/cron.php:859`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-10** — Implement `spawn_cron($gmt_time = 0)` returning `bool`.
  - Legacy source: `wp-includes/cron.php:899`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-11** — Implement `wp_cron()` returning `void`.
  - Legacy source: `wp-includes/cron.php:1020`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-12** — Implement `wp_get_schedules()` returning `array`.
  - Legacy source: `wp-includes/cron.php:1133`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-13** — Implement `wp_get_ready_cron_jobs()` returning `array`.
  - Legacy source: `wp-includes/cron.php:1215`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-14** — Implement `_get_cron_array()` returning `array`.
  - Legacy source: `wp-includes/cron.php:1261`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-15** — Implement `_set_cron_array($cron, $wp_error = false)` returning `bool|WP_Error`.
  - Legacy source: `wp-includes/cron.php:1297`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-16** — Enforce BR-CRON-01: All scheduled events live in the single autoloaded 'cron' option.
  - Legacy source: `wp-includes/cron.php:1261`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-17** — Enforce BR-CRON-02: Event identity is timestamp, then hook, then md5(serialize(args)).
  - Legacy source: `wp-includes/cron.php:132`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-18** — Enforce BR-CRON-03: Built-in schedules are hourly, twicedaily, daily and weekly.
  - Legacy source: `wp-includes/cron.php:1133`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-19** — Enforce BR-CRON-04: A single event duplicating another within a plus-or-minus 10-minute window is rejected.
  - Legacy source: `wp-includes/cron.php:135`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-20** — Enforce BR-CRON-05: For an event due within 10 minutes or already past, the lower bound becomes 0 so ALL past identical events count as duplicates, preventing accumulation of a repeatedly-failing event.
  - Legacy source: `wp-includes/cron.php:136`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-21** — Enforce BR-CRON-06: Cron cannot spawn while DOING_CRON is defined or the doing_wp_cron query arg is present.
  - Legacy source: `wp-includes/cron.php:903`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-22** — Enforce BR-CRON-07: A doing_cron lock more than 10 minutes in the future is discarded as invalid, self-healing against clock skew.
  - Legacy source: `wp-includes/cron.php:917`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-23** — Enforce BR-CRON-08: WP_CRON_LOCK_TIMEOUT (60 seconds) prevents spawning more than once a minute regardless of how many events are due.
  - Legacy source: `wp-includes/cron.php:921`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-24** — Enforce BR-CRON-09: Spawning aborts when no events are ready or the earliest ready timestamp is in the future.
  - Legacy source: `wp-includes/cron.php:927`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-25** — Enforce BR-CRON-10: ALTERNATE_WP_CRON runs cron via a visitor redirect and requires a GET request that is neither AJAX nor XML-RPC.
  - Legacy source: `wp-includes/cron.php:936`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-26** — Enforce BR-CRON-11: The default path issues a non-blocking loopback HTTP request to wp-cron.php.
  - Legacy source: `wp-includes/cron.php:960`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-27** — Enforce BR-CRON-12: The pre_schedule_event filter can replace scheduling entirely, which is how external cron systems take over.
  - Legacy source: `wp-includes/cron.php:100`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-28** — Enforce BR-CRON-13: _get_cron_array() migrates pre-version-2 cron arrays on read via _upgrade_cron_array().
  - Legacy source: `wp-includes/cron.php:1268`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

## Test Tasks

- [ ] **TT-01** — Happy-path test for the main flow (see `requirements.md` → Acceptance Criteria)
- [ ] **TT-02** — Failure-path test for the main error case
- [ ] **TT-03** — Assert BR-CRON-01 and its negative case
- [ ] **TT-04** — Assert BR-CRON-02 and its negative case
- [ ] **TT-05** — Assert BR-CRON-03 and its negative case
- [ ] **TT-06** — Assert BR-CRON-04 and its negative case
- [ ] **TT-07** — Assert BR-CRON-05 and its negative case
- [ ] **TT-08** — Assert BR-CRON-06 and its negative case
- [ ] **TT-09** — Assert BR-CRON-07 and its negative case
- [ ] **TT-10** — Assert BR-CRON-08 and its negative case
- [ ] **TT-11** — Assert every extension point in `design.md` can alter behaviour as documented

> ⚠️ **No legacy tests exist for this unit.** This checkout contains no test suite (`../inventory.md` §9, question Q1). Every test above must be written from the specification, not ported.

## Data Migration Tasks

- [ ] **TM-01** — Provision `cron option` per `../data-dictionary.md`
  - ⚠️ No foreign keys exist in the legacy schema; referential integrity is caller-enforced (`F-DD-01`)

## Suggested Order

1. Algorithms first (T-01…) — they encode the behaviour the functions expose.
2. Public functions next, in dependency order within the unit.
3. Business-rule enforcement last, with a test per rule.
4. **Blocker:** this unit cannot be validated until `options-and-transients`, `http-api`, `hooks-plugin-api`, `cache-and-object-cache` exist.

> ⚠️ **This unit is inside the 23-module dependency cycle** (`../traceability/spec-impact-matrix.md` §1). It cannot be fully validated in isolation; plan an integration checkpoint with the other members of the cycle.

