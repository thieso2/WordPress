# style-engine — Implementation Tasks

> Generated by the **Writer** (Reversa) · doc_level `complete`
> A sequence of executable tasks to reimplement this unit from the legacy system.
> Every task cites the legacy file the behaviour was extracted from.

---

## Prerequisites

- [ ] No internal dependencies — this unit can be built first
- [ ] Database schema matches `../data-dictionary.md` for: `WP_Style_Engine_CSS_Declarations`, `WP_Style_Engine_CSS_Rule`, `WP_Style_Engine_CSS_Rules_Store`
- [ ] PHP 7.4+ with the extensions listed in `../dependencies.md` §2

## Tasks

- [ ] **T-01** — Implement the *named rule stores* algorithm.
  - Behaviour: Block supports, global styles and layout each accumulate rules in independent named stores, rendered together and deduplicated by the processor.
  - Legacy source: `wp-includes/style-engine/class-wp-style-engine-css-rules-store.php`
  - Definition of done: the algorithm reproduces the cited behaviour exactly, including its edge conditions
  - Confidence: 🟢

- [ ] **T-02** — Implement `wp_style_engine_get_styles($block_styles, $options = array())` returning `array`.
  - Legacy source: `wp-includes/style-engine.php`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-03** — Implement `wp_style_engine_get_stylesheet_from_context($context, $options = array())` returning `string`.
  - Legacy source: `wp-includes/style-engine.php`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-04** — Implement `wp_style_engine_get_stylesheet_from_css_rules($css_rules, $options = array())` returning `string`.
  - Legacy source: `wp-includes/style-engine.php`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-05** — Enforce BR-SE-01: Declarations are sanitized when added, not when rendered.
  - Legacy source: `wp-includes/style-engine/class-wp-style-engine-css-declarations.php`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-06** — Enforce BR-SE-02: Multiple named rule stores can coexist and are rendered independently.
  - Legacy source: `wp-includes/style-engine/class-wp-style-engine-css-rules-store.php`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-07** — Enforce BR-SE-03: The processor deduplicates and combines rules before output.
  - Legacy source: `wp-includes/style-engine/class-wp-style-engine-processor.php`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-08** — Enforce BR-SE-04: WP_Style_Engine exposes both class names and inline styles for the same style object.
  - Legacy source: `wp-includes/style-engine/class-wp-style-engine.php`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

## Test Tasks

- [ ] **TT-01** — Happy-path test for the main flow (see `requirements.md` → Acceptance Criteria)
- [ ] **TT-02** — Failure-path test for the main error case
- [ ] **TT-03** — Assert BR-SE-01 and its negative case
- [ ] **TT-04** — Assert BR-SE-02 and its negative case
- [ ] **TT-05** — Assert BR-SE-03 and its negative case
- [ ] **TT-06** — Assert BR-SE-04 and its negative case
- [ ] **TT-07** — Assert every extension point in `design.md` can alter behaviour as documented

> ⚠️ **No legacy tests exist for this unit.** This checkout contains no test suite (`../inventory.md` §9, question Q1). Every test above must be written from the specification, not ported.

## Data Migration Tasks

- [ ] **TM-01** — Provision `WP_Style_Engine_CSS_Declarations` per `../data-dictionary.md`
  - ⚠️ No foreign keys exist in the legacy schema; referential integrity is caller-enforced (`F-DD-01`)
- [ ] **TM-02** — Provision `WP_Style_Engine_CSS_Rule` per `../data-dictionary.md`
  - ⚠️ No foreign keys exist in the legacy schema; referential integrity is caller-enforced (`F-DD-01`)
- [ ] **TM-03** — Provision `WP_Style_Engine_CSS_Rules_Store` per `../data-dictionary.md`
  - ⚠️ No foreign keys exist in the legacy schema; referential integrity is caller-enforced (`F-DD-01`)

## Suggested Order

1. Algorithms first (T-01…) — they encode the behaviour the functions expose.
2. Public functions next, in dependency order within the unit.
3. Business-rule enforcement last, with a test per rule.

