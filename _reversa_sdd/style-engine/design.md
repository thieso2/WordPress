# style-engine — Technical Design

> Generated by the **Writer** (Reversa) · doc_level `complete`
> HOW this unit is built, from the legacy code that was read.

---

## Interface

| Symbol | Signature | Returns | Source |
|--------|-----------|---------|--------|
| `wp_style_engine_get_styles` | `($block_styles, $options = array())` | `array` | `wp-includes/style-engine.php` |
| `wp_style_engine_get_stylesheet_from_context` | `($context, $options = array())` | `string` | `wp-includes/style-engine.php` |
| `wp_style_engine_get_stylesheet_from_css_rules` | `($css_rules, $options = array())` | `string` | `wp-includes/style-engine.php` |

## Main Flow

1. **named rule stores** — Block supports, global styles and layout each accumulate rules in independent named stores, rendered together and deduplicated by the processor.
   Source: `wp-includes/style-engine/class-wp-style-engine-css-rules-store.php` 🟢

## Alternative Flows

- **BR-SE-01:** Declarations are sanitized when added, not when rendered. 🟢

## Dependencies

_No internal dependencies. This unit is a foundation module._ 🟢

**Depended on by:** `block-supports`, `global-styles-theme-json`

## Identified Design Decisions

| Decision | Evidence in the code | Confidence |
|----------|----------------------|------------|
| Block supports, global styles and layout each accumulate rules in independent named stores, rendered together and deduplicated by the processor | `wp-includes/style-engine/class-wp-style-engine-css-rules-store.php` | 🟢 |
| Declarations are sanitized when added, not when rendered | `wp-includes/style-engine/class-wp-style-engine-css-declarations.php` | 🟢 |
| Multiple named rule stores can coexist and are rendered independently | `wp-includes/style-engine/class-wp-style-engine-css-rules-store.php` | 🟢 |
| The processor deduplicates and combines rules before output | `wp-includes/style-engine/class-wp-style-engine-processor.php` | 🟢 |
| WP_Style_Engine exposes both class names and inline styles for the same style object | `wp-includes/style-engine/class-wp-style-engine.php` | 🟢 |

## Internal State

### `WP_Style_Engine_CSS_Declarations`

- `declarations` : `array<property,value>` — sanitized on add

### `WP_Style_Engine_CSS_Rule`

- `selector` : `string`
- `declarations` : `WP_Style_Engine_CSS_Declarations`

### `WP_Style_Engine_CSS_Rules_Store`

- `name` : `string`
- `rules` : `WP_Style_Engine_CSS_Rule[]`

## Observability

_This unit emits no diagnostics of its own._ 🟡 Failures surface through its callers.

## Risks and Gaps

- 🟢 **F-SE-01** — The best-factored module in core: single responsibility per class, no globals, no hooks in the hot path.
- 🟢 **F-SE-02** — Returning either a class name or an inline style for the same input is what lets block supports choose between preset utility classes and custom values.

