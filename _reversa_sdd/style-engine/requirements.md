# style-engine

> Generated by the **Writer** (Reversa) · unit granularity `module` · doc_level `complete`
> Source: WordPress `7.2-alpha-63330` · extracted by the Archaeologist from the files listed below.
> Confidence on every statement: 🟢 CONFIRMED (read from code) · 🟡 INFERRED · 🔴 GAP

---

## Overview

Convert style objects into CSS rules. A small, focused module that both block supports and global styles depend on. 🟢

**Complexity:** `low` · **Business rules extracted:** 4 · **Public functions catalogued:** 3 · **Extension points:** 0

**Depended on by 2 module(s):** `block-supports`, `global-styles-theme-json`

## Responsibilities

- **named rule stores** — Block supports, global styles and layout each accumulate rules in independent named stores, rendered together and deduplicated by the processor 🟢

## Data owned

### `WP_Style_Engine_CSS_Declarations` 🟢

| Field | Type | Required | Note |
|-------|------|:--------:|------|
| `declarations` | `array<property,value>` | yes | sanitized on add |

### `WP_Style_Engine_CSS_Rule` 🟢

| Field | Type | Required | Note |
|-------|------|:--------:|------|
| `selector` | `string` | yes | — |
| `declarations` | `WP_Style_Engine_CSS_Declarations` | yes | — |

### `WP_Style_Engine_CSS_Rules_Store` 🟢

| Field | Type | Required | Note |
|-------|------|:--------:|------|
| `name` | `string` | yes | — |
| `rules` | `WP_Style_Engine_CSS_Rule[]` | yes | — |

## Business Rules

Every rule below was read from the source file cited. These are the contract.

| ID | Rule | Source | Confidence |
|----|------|--------|:----------:|
| `BR-SE-01` | Declarations are sanitized when added, not when rendered. | `wp-includes/style-engine/class-wp-style-engine-css-declarations.php` | 🟢 |
| `BR-SE-02` | Multiple named rule stores can coexist and are rendered independently. | `wp-includes/style-engine/class-wp-style-engine-css-rules-store.php` | 🟢 |
| `BR-SE-03` | The processor deduplicates and combines rules before output. | `wp-includes/style-engine/class-wp-style-engine-processor.php` | 🟢 |
| `BR-SE-04` | WP_Style_Engine exposes both class names and inline styles for the same style object. | `wp-includes/style-engine/class-wp-style-engine.php` | 🟢 |

## Functional Requirements

| ID | Requirement | Priority | Acceptance criterion |
|----|-------------|----------|----------------------|
| FR-01 | Declarations are sanitized when added, not when rendered | Must | Verify at `wp-includes/style-engine/class-wp-style-engine-css-declarations.php` |
| FR-02 | Multiple named rule stores can coexist and are rendered independently | Must | Verify at `wp-includes/style-engine/class-wp-style-engine-css-rules-store.php` |
| FR-03 | The processor deduplicates and combines rules before output | Should | Verify at `wp-includes/style-engine/class-wp-style-engine-processor.php` |
| FR-04 | WP_Style_Engine exposes both class names and inline styles for the same style object | Should | Verify at `wp-includes/style-engine/class-wp-style-engine.php` |

## Non-Functional Requirements

| Type | Inferred requirement | Evidence in the code | Confidence |
|------|----------------------|----------------------|------------|
| Security | Declarations are sanitized when added, not when rendered | `wp-includes/style-engine/class-wp-style-engine-css-declarations.php` | 🟢 |

> Inferred from code evidence only. Lines with no traceable evidence were omitted.

## Acceptance Criteria

```gherkin
# BR-SE-01
Given the style-engine module is loaded
When the behaviour at wp-includes/style-engine/class-wp-style-engine-css-declarations.php is exercised
Then Declarations are sanitized when added, not when rendered

# BR-SE-02
Given the style-engine module is loaded
When the behaviour at wp-includes/style-engine/class-wp-style-engine-css-rules-store.php is exercised
Then Multiple named rule stores can coexist and are rendered independently

# BR-SE-03
Given the style-engine module is loaded
When the behaviour at wp-includes/style-engine/class-wp-style-engine-processor.php is exercised
Then The processor deduplicates and combines rules before output

# BR-SE-04
Given the style-engine module is loaded
When the behaviour at wp-includes/style-engine/class-wp-style-engine.php is exercised
Then WP_Style_Engine exposes both class names and inline styles for the same style object

# Failure path
Given an input that violates the constraints above
When the operation is attempted
Then the module must fail in the manner its rules describe
And must not silently produce a partial result
```

## Priority (MoSCoW)

| Requirement | MoSCoW | Rationale |
|-------------|--------|-----------|
| Core responsibilities | Must | 2 module(s) depend on this unit |
| `BR-SE-03` | Should | Secondary rule; 2 module(s) depend on this unit |
| `BR-SE-04` | Should | Secondary rule; 2 module(s) depend on this unit |

> Priority inferred from dependent count (`traceability/spec-impact-matrix.md`) and position in the dependency chain.

## Code Traceability

| File | Function / Class | Coverage |
|------|------------------|----------|
| `wp-includes/style-engine.php` | `wp_style_engine_get_styles`, `wp_style_engine_get_stylesheet_from_context`, `wp_style_engine_get_stylesheet_from_css_rules` | 🟢 |
| `wp-includes/style-engine/class-wp-style-engine.php` | _module file_ | 🟡 |
| `wp-includes/style-engine/class-wp-style-engine-css-declarations.php` | _module file_ | 🟡 |
| `wp-includes/style-engine/class-wp-style-engine-css-rule.php` | _module file_ | 🟡 |
| `wp-includes/style-engine/class-wp-style-engine-css-rules-store.php` | _module file_ | 🟡 |
| `wp-includes/style-engine/class-wp-style-engine-processor.php` | _module file_ | 🟡 |

