# blocks-library

> Generated by the **Writer** (Reversa) · unit granularity `module` · doc_level `complete`
> Source: WordPress `7.2-alpha-63330` · extracted by the Archaeologist from the files listed below.
> Confidence on every statement: 🟢 CONFIRMED (read from code) · 🟡 INFERRED · 🔴 GAP

---

## Overview

The 115 built-in blocks. Each is a block.json plus, for dynamic blocks, a PHP render callback. 🟢

**Complexity:** `medium` · **Business rules extracted:** 6 · **Public functions catalogued:** 0 · **Extension points:** 2

**Depended on by 0 module(s):** _none — terminal module_

## Responsibilities


## Data owned

### `core block directory` 🟢

| Field | Type | Required | Note |
|-------|------|:--------:|------|
| `block.json` | `metadata file` | yes | — |
| `{name}.php` | `render callback, dynamic blocks only` | no | — |
| `style.css / editor.css / view.js` | `compiled assets` | no | — |

## Business Rules

Every rule below was read from the source file cited. These are the contract.

| ID | Rule | Source | Confidence |
|----|------|--------|:----------:|
| `BR-BLIB-01` | Every core block is defined by a block.json file. | `wp-includes/blocks/<block>/block.json` | 🟢 |
| `BR-BLIB-02` | All core block.json contents are inlined into blocks-json.php as a single 206 KB PHP array, collapsing 115 file reads into one require. | `wp-includes/blocks/blocks-json.php` | 🟢 |
| `BR-BLIB-03` | Blocks with a PHP file are dynamic; those without render from saved markup. | `wp-includes/blocks/` | 🟢 |
| `BR-BLIB-04` | The 'missing' block preserves the original content when a block type is not registered. It is a STATIC block — wp-includes/blocks/missing/ contains only block.json; the placeholder is rendered client-side by the editor, not by PHP. | `wp-includes/blocks/missing/block.json` | 🟢 |
| `BR-BLIB-05` | The 'freeform' block wraps pre-block classic editor content. It is a STATIC block — wp-includes/blocks/freeform/ contains block.json and editor CSS only. The wrapping happens in blocks.php, not in a render callback. | `wp-includes/blocks.php:1293` | 🟢 |
| `BR-BLIB-06` | Post-context blocks (post-title, post-content, etc.) read from the current query context rather than from attributes. | `wp-includes/blocks/post-title.php (30 post-* blocks)` | 🟢 |

## Functional Requirements

| ID | Requirement | Priority | Acceptance criterion |
|----|-------------|----------|----------------------|
| FR-01 | Every core block is defined by a block.json file | Must | Verify at `wp-includes/blocks/<block>/block.json` |
| FR-02 | All core block.json contents are inlined into blocks-json.php as a single 206 KB PHP array, collapsing 115 file reads into one require | Must | Verify at `wp-includes/blocks/blocks-json.php` |
| FR-03 | Blocks with a PHP file are dynamic; those without render from saved markup | Should | Verify at `wp-includes/blocks/` |
| FR-04 | The 'missing' block preserves the original content when a block type is not registered. It is a STATIC block — wp-includes/blocks/missing/ contains only block.json; the placeholder is rendered client-side by the editor, not by PHP | Should | Verify at `wp-includes/blocks/missing/block.json` |
| FR-05 | The 'freeform' block wraps pre-block classic editor content. It is a STATIC block — wp-includes/blocks/freeform/ contains block.json and editor CSS only. The wrapping happens in blocks.php, not in a render callback | Should | Verify at `wp-includes/blocks.php:1293` |
| FR-06 | Post-context blocks (post-title, post-content, etc.) read from the current query context rather than from attributes | Should | Verify at `wp-includes/blocks/post-title.php (30 post-* blocks)` |

## Non-Functional Requirements

| Type | Inferred requirement | Evidence in the code | Confidence |
|------|----------------------|----------------------|------------|
| Security | Blocks now cover comments, queries, navigation and site identity, functionality previously owned by templates, widgets and menus: four systems… | — | 🟢 |
| Availability | The missing block is a graceful-degradation mechanism preserving content when a block-providing plugin is deactivated, the block equivalent of… | — | 🟢 |

> Inferred from code evidence only. Lines with no traceable evidence were omitted.

## Acceptance Criteria

```gherkin
# BR-BLIB-01
Given the blocks-library module is loaded
When the behaviour at wp-includes/blocks/<block>/block.json is exercised
Then Every core block is defined by a block.json file

# BR-BLIB-02
Given the blocks-library module is loaded
When the behaviour at wp-includes/blocks/blocks-json.php is exercised
Then All core block.json contents are inlined into blocks-json.php as a single 206 KB PHP array, collapsing 115 file reads into one require

# BR-BLIB-03
Given the blocks-library module is loaded
When the behaviour at wp-includes/blocks/ is exercised
Then Blocks with a PHP file are dynamic; those without render from saved markup

# BR-BLIB-04
Given the blocks-library module is loaded
When the behaviour at wp-includes/blocks/missing/block.json is exercised
Then The 'missing' block preserves the original content when a block type is not registered. It is a STATIC block — wp-includes/blocks/missing/ contains only block.json; the placeholder is rendered client-side by the editor, not by PHP

# BR-BLIB-05
Given the blocks-library module is loaded
When the behaviour at wp-includes/blocks.php:1293 is exercised
Then The 'freeform' block wraps pre-block classic editor content. It is a STATIC block — wp-includes/blocks/freeform/ contains block.json and editor CSS only. The wrapping happens in blocks.php, not in a render callback

# BR-BLIB-06
Given the blocks-library module is loaded
When the behaviour at wp-includes/blocks/post-title.php (30 post-* blocks) is exercised
Then Post-context blocks (post-title, post-content, etc.) read from the current query context rather than from attributes

# Failure path
Given an input that violates the constraints above
When the operation is attempted
Then the module must fail in the manner its rules describe
And must not silently produce a partial result
```

## Priority (MoSCoW)

| Requirement | MoSCoW | Rationale |
|-------------|--------|-----------|
| Core responsibilities | Must | terminal module — no other unit depends on it |
| `BR-BLIB-03` | Should | Secondary rule; terminal module — no other unit depends on it |
| `BR-BLIB-04` | Should | Secondary rule; terminal module — no other unit depends on it |
| `BR-BLIB-05` | Should | Secondary rule; terminal module — no other unit depends on it |
| `BR-BLIB-06` | Should | Secondary rule; terminal module — no other unit depends on it |

> Priority inferred from dependent count (`traceability/spec-impact-matrix.md`) and position in the dependency chain.

## Code Traceability

| File | Function / Class | Coverage |
|------|------------------|----------|
| `wp-includes/blocks/` | _module file_ | 🟡 |
| `wp-includes/blocks/blocks-json.php` | _module file_ | 🟡 |
| `wp-includes/blocks/index.php` | _module file_ | 🟡 |
| `wp-includes/blocks/require-dynamic-blocks.php` | _module file_ | 🟡 |

