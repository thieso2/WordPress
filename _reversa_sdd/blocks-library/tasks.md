# blocks-library — Implementation Tasks

> Generated by the **Writer** (Reversa) · doc_level `complete`
> A sequence of executable tasks to reimplement this unit from the legacy system.
> Every task cites the legacy file the behaviour was extracted from.

---

## Prerequisites

- [ ] Unit `block-editor` is available (see `design.md` → Dependencies)
- [ ] Unit `block-supports` is available (see `design.md` → Dependencies)
- [ ] Unit `query-and-loop` is available (see `design.md` → Dependencies)
- [ ] Unit `posts-and-post-types` is available (see `design.md` → Dependencies)
- [ ] Unit `comments` is available (see `design.md` → Dependencies)
- [ ] Unit `taxonomy-and-terms` is available (see `design.md` → Dependencies)
- [ ] Unit `interactivity-api` is available (see `design.md` → Dependencies)
- [ ] Database schema matches `../data-dictionary.md` for: `core block directory`
- [ ] PHP 7.4+ with the extensions listed in `../dependencies.md` §2

## Tasks

- [ ] **T-01** — Enforce BR-BLIB-01: Every core block is defined by a block.json file.
  - Legacy source: `wp-includes/blocks/<block>/block.json`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-02** — Enforce BR-BLIB-02: All core block.json contents are inlined into blocks-json.php as a single 206 KB PHP array, collapsing 115 file reads into one require.
  - Legacy source: `wp-includes/blocks/blocks-json.php`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-03** — Enforce BR-BLIB-03: Blocks with a PHP file are dynamic; those without render from saved markup.
  - Legacy source: `wp-includes/blocks/`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-04** — Enforce BR-BLIB-04: The 'missing' block preserves the original content when a block type is not registered. It is a STATIC block — wp-includes/blocks/missing/ contains only block.json; the placeholder is rendered client-side by the editor, not by PHP.
  - Legacy source: `wp-includes/blocks/missing/block.json`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-05** — Enforce BR-BLIB-05: The 'freeform' block wraps pre-block classic editor content. It is a STATIC block — wp-includes/blocks/freeform/ contains block.json and editor CSS only. The wrapping happens in blocks.php, not in a render callback.
  - Legacy source: `wp-includes/blocks.php:1293`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-06** — Enforce BR-BLIB-06: Post-context blocks (post-title, post-content, etc.) read from the current query context rather than from attributes.
  - Legacy source: `wp-includes/blocks/post-title.php (30 post-* blocks)`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

## Test Tasks

- [ ] **TT-01** — Happy-path test for the main flow (see `requirements.md` → Acceptance Criteria)
- [ ] **TT-02** — Failure-path test for the main error case
- [ ] **TT-03** — Assert BR-BLIB-01 and its negative case
- [ ] **TT-04** — Assert BR-BLIB-02 and its negative case
- [ ] **TT-05** — Assert BR-BLIB-03 and its negative case
- [ ] **TT-06** — Assert BR-BLIB-04 and its negative case
- [ ] **TT-07** — Assert BR-BLIB-05 and its negative case
- [ ] **TT-08** — Assert BR-BLIB-06 and its negative case
- [ ] **TT-09** — Assert every extension point in `design.md` can alter behaviour as documented

> ⚠️ **No legacy tests exist for this unit.** This checkout contains no test suite (`../inventory.md` §9, question Q1). Every test above must be written from the specification, not ported.

## Data Migration Tasks

- [ ] **TM-01** — Provision `core block directory` per `../data-dictionary.md`
  - ⚠️ No foreign keys exist in the legacy schema; referential integrity is caller-enforced (`F-DD-01`)

## Suggested Order

1. Algorithms first (T-01…) — they encode the behaviour the functions expose.
2. Public functions next, in dependency order within the unit.
3. Business-rule enforcement last, with a test per rule.
4. **Blocker:** this unit cannot be validated until `block-editor`, `block-supports`, `query-and-loop`, `posts-and-post-types` exist.

