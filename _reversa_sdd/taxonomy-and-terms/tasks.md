# taxonomy-and-terms — Implementation Tasks

> Generated by the **Writer** (Reversa) · doc_level `complete`
> A sequence of executable tasks to reimplement this unit from the legacy system.
> Every task cites the legacy file the behaviour was extracted from.

---

## Prerequisites

- [ ] Unit `database-wpdb` is available (see `design.md` → Dependencies)
- [ ] Unit `metadata` is available (see `design.md` → Dependencies)
- [ ] Unit `cache-and-object-cache` is available (see `design.md` → Dependencies)
- [ ] Unit `hooks-plugin-api` is available (see `design.md` → Dependencies)
- [ ] Unit `posts-and-post-types` is available (see `design.md` → Dependencies)
- [ ] Unit `formatting-and-sanitization` is available (see `design.md` → Dependencies)
- [ ] Database schema matches `../data-dictionary.md` for: `wp_terms`, `wp_term_taxonomy`, `wp_term_relationships`
- [ ] PHP 7.4+ with the extensions listed in `../dependencies.md` §2

## Tasks

- [ ] **T-01** — Implement the *insert-then-detect-duplicate* algorithm.
  - Behaviour: Rows are inserted first, then a SELECT looks for an older row (term_id < new id) with the same slug/parent/taxonomy. If found, the new rows are deleted and the pre-existing ids returned. A compensating transaction implemented without transactions.
  - Legacy source: `wp-includes/taxonomy.php:2645`
  - Definition of done: the algorithm reproduces the cited behaviour exactly, including its edge conditions
  - Confidence: 🟢

- [ ] **T-02** — Implement the *type-dependent term creation* algorithm.
  - Behaviour: In wp_set_object_terms() a string term that does not exist is created via wp_insert_term(); an integer term that does not exist is skipped with no error.
  - Legacy source: `wp-includes/taxonomy.php:2884`
  - Definition of done: the algorithm reproduces the cited behaviour exactly, including its edge conditions
  - Confidence: 🟢

- [ ] **T-03** — Implement the *static-accumulator deferred counting* algorithm.
  - Behaviour: wp_defer_term_counting(true) queues term ids in a static array keyed by taxonomy, flushed in one pass by wp_update_term_count($x,$y,true).
  - Legacy source: `wp-includes/taxonomy.php:3587`
  - Definition of done: the algorithm reproduces the cited behaviour exactly, including its edge conditions
  - Confidence: 🟢

- [ ] **T-04** — Implement the *hierarchical count padding* algorithm.
  - Behaviour: One join over term_relationships x posts filtered to publish status, building term_items[term_id][object_id]; ancestors are walked with a cycle guard and object ids copied up, deduplicating so a post in child and parent counts once.
  - Legacy source: `wp-includes/taxonomy.php:4085`
  - Definition of done: the algorithm reproduces the cited behaviour exactly, including its edge conditions
  - Confidence: 🟢

- [ ] **T-05** — Implement the *term_group aliasing* algorithm.
  - Behaviour: alias_of joins an existing group or allocates MAX(term_group)+1 and moves the target term into it.
  - Legacy source: `wp-includes/taxonomy.php:2506`
  - Definition of done: the algorithm reproduces the cited behaviour exactly, including its edge conditions
  - Confidence: 🟢

- [ ] **T-06** — Implement `create_initial_taxonomies()` returning `void`.
  - Legacy source: `wp-includes/taxonomy.php:25`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-07** — Implement `register_taxonomy($taxonomy, $object_type, $args = array())` returning `WP_Taxonomy|WP_Error`.
  - Legacy source: `wp-includes/taxonomy.php:518`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-08** — Implement `get_term($term, $taxonomy = '', $output = OBJECT, $filter = 'raw')` returning `WP_Term|array|WP_Error|null`.
  - Legacy source: `wp-includes/taxonomy.php:977`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-09** — Implement `get_terms($args = array(), $deprecated = '')` returning `WP_Term[]|int[]|string|WP_Error`.
  - Legacy source: `wp-includes/taxonomy.php:1316`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-10** — Implement `term_exists($term, $taxonomy = '', $parent_term = null)` returning `mixed`.
  - Legacy source: `wp-includes/taxonomy.php:1605`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-11** — Implement `wp_insert_term($term, $taxonomy, $args = array())` returning `array|WP_Error`.
  - Legacy source: `wp-includes/taxonomy.php:2458`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-12** — Implement `wp_update_term($term_id, $taxonomy, $args = array())` returning `array|WP_Error`.
  - Legacy source: `wp-includes/taxonomy.php:3262`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-13** — Implement `wp_delete_term($term, $taxonomy, $args = array())` returning `bool|int|WP_Error`.
  - Legacy source: `wp-includes/taxonomy.php:2057`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-14** — Implement `wp_set_object_terms($object_id, $terms, $taxonomy, $append = false)` returning `array|WP_Error`.
  - Legacy source: `wp-includes/taxonomy.php:2851`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-15** — Implement `wp_get_object_terms($object_ids, $taxonomies, $args = array())` returning `WP_Term[]|WP_Error`.
  - Legacy source: `wp-includes/taxonomy.php:2294`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-16** — Implement `wp_unique_term_slug($slug, $term)` returning `string`.
  - Legacy source: `wp-includes/taxonomy.php:3136`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-17** — Implement `wp_update_term_count($terms, $taxonomy, $do_deferred = false)` returning `bool`.
  - Legacy source: `wp-includes/taxonomy.php:3587`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-18** — Implement `_pad_term_counts(&$terms, $taxonomy)` returning `void`.
  - Legacy source: `wp-includes/taxonomy.php:4085`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-19** — Implement `get_ancestors($object_id = 0, $object_type = '', $resource_type = '')` returning `int[]`.
  - Legacy source: `wp-includes/taxonomy.php:5036`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-20** — Enforce BR-TAX-01: Objects are linked by term_taxonomy_id, never term_id. One term_id may serve several taxonomies.
  - Legacy source: `schema`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-21** — Enforce BR-TAX-02: The term name is validated for emptiness twice: before and after sanitization.
  - Legacy source: `wp-includes/taxonomy.php:2470`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-22** — Enforce BR-TAX-03: A null description is coerced to empty string to avoid a database error.
  - Legacy source: `wp-includes/taxonomy.php:2487`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-23** — Enforce BR-TAX-04: A non-existent parent term id rejects the insert with the missing_parent error.
  - Legacy source: `wp-includes/taxonomy.php:2481`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-24** — Enforce BR-TAX-05: Duplicate detection happens after insert; only a row with a lower term_id counts as the duplicate, and the newly inserted rows are then deleted.
  - Legacy source: `wp-includes/taxonomy.php:2645`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-25** — Enforce BR-TAX-06: In wp_set_object_terms(), a non-existent term passed as a string is created; passed as an integer it is silently skipped.
  - Legacy source: `wp-includes/taxonomy.php:2884`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-26** — Enforce BR-TAX-07: Assigning a term already related to the object is a no-op, checked with one query per term.
  - Legacy source: `wp-includes/taxonomy.php:2893`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-27** — Enforce BR-TAX-08: With $append = false, terms present before but absent from the new list are removed.
  - Legacy source: `wp-includes/taxonomy.php:2864`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-28** — Enforce BR-TAX-09: Term counts can be deferred with wp_defer_term_counting(true) and accumulate in a static keyed by taxonomy.
  - Legacy source: `wp-includes/taxonomy.php:3589`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-29** — Enforce BR-TAX-10: A taxonomy's update_count_callback overrides the default counting query.
  - Legacy source: `wp-includes/taxonomy.php:3616`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-30** — Enforce BR-TAX-11: Hierarchical count padding counts only objects with post_status = 'publish'.
  - Legacy source: `wp-includes/taxonomy.php:4106`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-31** — Enforce BR-TAX-12: Count padding deduplicates by object id, so a post in both a child and its parent is counted once for the parent.
  - Legacy source: `wp-includes/taxonomy.php:4108`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-32** — Enforce BR-TAX-13: Ancestor walking has an explicit cycle guard against corrupted parent chains.
  - Legacy source: `wp-includes/taxonomy.php:4122`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-33** — Enforce BR-TAX-14: alias_of groups terms via term_group, allocated as MAX(term_group) + 1.
  - Legacy source: `wp-includes/taxonomy.php:2506`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

## Test Tasks

- [ ] **TT-01** — Happy-path test for the main flow (see `requirements.md` → Acceptance Criteria)
- [ ] **TT-02** — Failure-path test for the main error case
- [ ] **TT-03** — Assert BR-TAX-01 and its negative case
- [ ] **TT-04** — Assert BR-TAX-02 and its negative case
- [ ] **TT-05** — Assert BR-TAX-03 and its negative case
- [ ] **TT-06** — Assert BR-TAX-04 and its negative case
- [ ] **TT-07** — Assert BR-TAX-05 and its negative case
- [ ] **TT-08** — Assert BR-TAX-06 and its negative case
- [ ] **TT-09** — Assert BR-TAX-07 and its negative case
- [ ] **TT-10** — Assert BR-TAX-08 and its negative case
- [ ] **TT-11** — Assert every extension point in `design.md` can alter behaviour as documented

> ⚠️ **No legacy tests exist for this unit.** This checkout contains no test suite (`../inventory.md` §9, question Q1). Every test above must be written from the specification, not ported.

## Data Migration Tasks

- [ ] **TM-01** — Provision `wp_terms` per `../data-dictionary.md`
  - ⚠️ No foreign keys exist in the legacy schema; referential integrity is caller-enforced (`F-DD-01`)
- [ ] **TM-02** — Provision `wp_term_taxonomy` per `../data-dictionary.md`
  - ⚠️ No foreign keys exist in the legacy schema; referential integrity is caller-enforced (`F-DD-01`)
- [ ] **TM-03** — Provision `wp_term_relationships` per `../data-dictionary.md`
  - ⚠️ No foreign keys exist in the legacy schema; referential integrity is caller-enforced (`F-DD-01`)

## Suggested Order

1. Algorithms first (T-01…) — they encode the behaviour the functions expose.
2. Public functions next, in dependency order within the unit.
3. Business-rule enforcement last, with a test per rule.
4. **Blocker:** this unit cannot be validated until `database-wpdb`, `metadata`, `cache-and-object-cache`, `hooks-plugin-api` exist.

> ⚠️ **This unit is inside the 23-module dependency cycle** (`../traceability/spec-impact-matrix.md` §1). It cannot be fully validated in isolation; plan an integration checkpoint with the other members of the cycle.

