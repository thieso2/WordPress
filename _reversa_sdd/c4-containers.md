# C4 Level 2 — Containers

> Generated by the **Architect** (Reversa) · doc_level `complete`
> Confidence: 🟢 CONFIRMED unless marked

## The honest picture

WordPress is a **single deployable unit**: PHP files served by a web server, talking to one MySQL
database. There are no services, no queues, no workers. "Containers" here means the deployment
artifacts that actually exist.

```mermaid
C4Container
    title Containers — WordPress

    Person(user, "User", "Any of 6 roles")
    Person_Ext(agent, "AI Agent", "")

    System_Boundary(wp, "WordPress installation") {
        Container(front, "Front-end application", "PHP", "index.php → wp-blog-header.php → WP::main() → template-loader.php. Renders themes.")
        Container(admin, "Admin application", "PHP", "wp-admin/ — 95 screens, 106 includes. Single auth gate at admin.php:104.")
        Container(rest, "REST API", "PHP", "/wp-json/ — 49 controllers, schema-driven validate→sanitize→permit.")
        Container(xmlrpc, "XML-RPC endpoint", "PHP", "xmlrpc.php — 78 methods, enabled by default, credentials per call.")
        Container(ajax, "Admin-Ajax", "PHP", "admin-ajax.php — action-name dispatch, no declarative authorization.")
        Container(cron, "Cron runner", "PHP", "wp-cron.php — invoked by loopback HTTP from visitor traffic.")
        Container(kernel, "Core kernel", "PHP", "wp-includes/ — 44 modules. Hooks, wpdb, query, capabilities, blocks.")
        ContainerDb(db, "MySQL / MariaDB", "MySQL 5.5.5+", "18 tables. Permissive SQL mode enforced per session.")
        Container(cache, "Object cache", "PHP array or drop-in", "REQUEST-SCOPED by default. object-cache.php replaces it.")
        Container(fs, "Filesystem", "Local / FTP / SSH", "wp-content/uploads, plugins, themes. Method chosen by file-owner probe.")
    }

    System_Ext(wporg, "api.wordpress.org", "")
    System_Ext(ext, "oEmbed · Feeds · SMTP · AI providers", "")

    Rel(user, front, "Reads", "HTTPS")
    Rel(user, admin, "Administers", "HTTPS")
    Rel(user, rest, "Uses editor / apps", "HTTPS")
    Rel(agent, rest, "Invokes Abilities", "HTTPS")

    Rel(front, kernel, "Uses")
    Rel(admin, kernel, "Uses")
    Rel(rest, kernel, "Uses")
    Rel(xmlrpc, kernel, "Uses")
    Rel(ajax, kernel, "Uses")
    Rel(cron, kernel, "Uses")

    Rel(kernel, db, "All reads/writes via $wpdb", "mysqli")
    Rel(kernel, cache, "wp_cache_* ")
    Rel(kernel, fs, "WP_Filesystem")
    Rel(kernel, wporg, "Update checks", "HTTPS")
    Rel(kernel, ext, "Outbound requests", "HTTPS via WP_Http")

    Rel(front, cron, "Spawns when work is due", "non-blocking loopback HTTP")
```

## Entry points 🟢

Every one of these boots the full kernel via `wp-load.php` → `wp-settings.php` (311 requires).

| Entry point | Purpose | Auth gate |
|-------------|---------|-----------|
| `index.php` | front-end | none |
| `wp-admin/admin.php` | admin bootstrap | `auth_redirect()` |
| `wp-includes/rest-api.php` → `/wp-json/` | REST | per-route `permission_callback` ⚠️ absent ⇒ public |
| `wp-admin/admin-ajax.php` | AJAX | ⚠️ `nopriv` ⇒ public, no gate |
| `wp-admin/admin-post.php` | form POSTs | per-handler |
| `xmlrpc.php` | XML-RPC | credentials per call |
| `wp-login.php` | authentication | — |
| `wp-cron.php` | scheduler | `doing_cron` transient lock |
| `wp-comments-post.php` | comment submission | duplicate + flood + moderation |
| `wp-trackback.php` | trackbacks | none |
| `wp-signup.php` / `wp-activate.php` | multisite registration | — |
| `wp-mail.php` | post-by-email | POP3 credentials |
| `wp-links-opml.php` | OPML export | none |

## Replaceable containers — the drop-ins 🟢

Five files in `wp-content/` replace a container wholesale, **loaded by filename with no interface
validation** (F-BOOT-03):

| Drop-in | Replaces | Loaded at |
|---------|----------|-----------|
| `db.php` | the entire `wpdb` class | `wp-settings.php:136` |
| `object-cache.php` | `WP_Object_Cache` | `wp-settings.php:151` |
| `advanced-cache.php` | full-page caching | `wp-settings.php:99` |
| `sunrise.php` | network resolution (domain mapping) | `ms-settings.php` |
| `fatal-error-handler.php` | the error template | `class-wp-fatal-error-handler.php:144` |

**`object-cache.php` is the highest-leverage file in a WordPress deployment.** Its presence changes
transient storage (BR-OPT-10), cron lock semantics (F-CRON-02) and the `WP_Query` split-query
optimization (BR-QUERY-07).

## What is absent 🟢

| Expected container | Present? | Consequence |
|--------------------|----------|-------------|
| Application server / worker pool | ❌ | No background processing; cron rides visitor traffic (ADR-006) |
| Message queue | ❌ | The `cron` option is the queue |
| Search index | ❌ | Search is `LIKE '%term%'` (F-QUERY-04) |
| Persistent cache | ❌ by default | Request-scoped array (F-CACHE-01) |
| CDN / asset pipeline | ❌ | Assets served from `wp-includes` / `wp-content` |
| Container definition | ❌ | No Dockerfile in this tree |
| CI pipeline | ❌ | No `.github/` in this tree 🔴 |

## Request flows 🟢

```mermaid
sequenceDiagram
    autonumber
    participant B as Browser
    participant I as index.php
    participant S as wp-settings.php
    participant W as WP::main()
    participant Q as WP_Query
    participant D as MySQL
    participant T as template-loader.php

    B->>I: GET /hello-world/
    I->>S: wp-load.php → 311 requires
    S->>S: wp_check_php_mysql_versions()
    S->>D: connect, strip 6 SQL modes
    S->>S: object cache, default filters
    S->>S: mu-plugins → network → active plugins
    Note over S: do_action('plugins_loaded')
    S->>S: wp_magic_quotes() — input re-slashed
    S->>S: create $wp_query, $wp_rewrite, $wp, $wp_roles
    Note over S: do_action('setup_theme')
    S->>S: child then parent functions.php
    Note over S: do_action('after_setup_theme')
    S->>S: $wp->init() — current user
    Note over S: do_action('init')
    Note over S: do_action('wp_loaded')

    I->>W: wp()
    W->>W: parse_request() — match rewrite_rules option
    W->>Q: query_posts()
    Q->>D: SELECT (34 filters applied to the SQL)
    D-->>Q: rows
    Q->>D: SELECT FOUND_ROWS() unless no_found_rows
    W->>W: handle_404() — AFTER the query has run
    W->>W: register_globals(), send_headers()
    Note over W: do_action('wp')

    I->>T: template-loader.php
    Note over T: do_action('template_redirect')
    T->>T: 17 conditionals → get_single_template()
    T->>T: locate_template() — child → parent → theme-compat
    T->>B: rendered HTML

    opt cron work due
        T->>T: spawn_cron() → non-blocking POST to wp-cron.php
    end
```
