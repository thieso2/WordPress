# updates-and-upgrader — Implementation Tasks

> Generated by the **Writer** (Reversa) · doc_level `complete`
> A sequence of executable tasks to reimplement this unit from the legacy system.
> Every task cites the legacy file the behaviour was extracted from.

---

## Prerequisites

- [ ] Unit `http-api` is available (see `design.md` → Dependencies)
- [ ] Unit `filesystem-api` is available (see `design.md` → Dependencies)
- [ ] Unit `options-and-transients` is available (see `design.md` → Dependencies)
- [ ] Unit `database-wpdb` is available (see `design.md` → Dependencies)
- [ ] Unit `cron` is available (see `design.md` → Dependencies)
- [ ] Unit `bootstrap-and-load` is available (see `design.md` → Dependencies)
- [ ] PHP 7.4+ with the extensions listed in `../dependencies.md` §2

## Tasks

- [ ] **T-01** — Implement the *escalating check backoff* algorithm.
  - Behaviour: Timeout rises from 1 minute during cron through 1 hour and 2 hours to 12 hours in steady state, so a failing update endpoint costs one slow request per hour rather than one per page view.
  - Legacy source: `wp-includes/update.php:383`
  - Definition of done: the algorithm reproduces the cited behaviour exactly, including its edge conditions
  - Confidence: 🟢

- [ ] **T-02** — Implement the *diff-based schema migration* algorithm.
  - Behaviour: dbDelta() compares wp_get_db_schema()'s CREATE TABLE statements with the live schema and emits ALTER statements; data migrations that cannot be expressed as schema shape live in version-gated upgrade_NNN() functions.
  - Legacy source: `wp-admin/includes/upgrade.php`
  - Definition of done: the algorithm reproduces the cited behaviour exactly, including its edge conditions
  - Confidence: 🟢

- [ ] **T-03** — Implement the *download-verify-unpack-install pipeline* algorithm.
  - Behaviour: download_package with signature verification, unpack via ZipArchive or PclZip, install_package under maintenance mode, with per-version file manifests for core cleanup.
  - Legacy source: `wp-admin/includes/class-wp-upgrader.php`
  - Definition of done: the algorithm reproduces the cited behaviour exactly, including its edge conditions
  - Confidence: 🟢

- [ ] **T-04** — Implement `wp_version_check($extra_stats = array(), $force_check = false)` returning `void|false`.
  - Legacy source: `wp-includes/update.php:59`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-05** — Implement `wp_update_plugins($extra_stats = array())` returning `void|false`.
  - Legacy source: `wp-includes/update.php:383`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-06** — Implement `wp_update_themes($extra_stats = array())` returning `void|false`.
  - Legacy source: `wp-includes/update.php:670`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-07** — Implement `WP_Upgrader::download_package($package, $check_signatures = false, $hook_extra = array())` returning `string|WP_Error`.
  - Legacy source: `wp-admin/includes/class-wp-upgrader.php`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-08** — Implement `WP_Upgrader::unpack_package($package, $delete_package = true)` returning `string|WP_Error`.
  - Legacy source: `wp-admin/includes/class-wp-upgrader.php`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-09** — Implement `WP_Upgrader::install_package($args = array())` returning `array|WP_Error`.
  - Legacy source: `wp-admin/includes/class-wp-upgrader.php`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-10** — Implement `dbDelta($queries = '', $execute = true)` returning `array`.
  - Legacy source: `wp-admin/includes/upgrade.php`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-11** — Implement `wp_get_db_schema($scope = 'all', $blog_id = null)` returning `string`.
  - Legacy source: `wp-admin/includes/schema.php`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-12** — Enforce BR-UPD-01: Update data is cached in the update_core, update_plugins and update_themes site transients.
  - Legacy source: `wp-includes/update.php`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-13** — Enforce BR-UPD-02: The check timeout escalates: 1 minute during cron, 1 hour after a recent attempt, 2 hours, then 12 hours in steady state.
  - Legacy source: `wp-includes/update.php:383`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-14** — Enforce BR-UPD-03: A re-check is suppressed within 12 hours of last_checked.
  - Legacy source: `wp-includes/update.php:1033`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-15** — Enforce BR-UPD-04: Packages are signature-verified after download and before extraction.
  - Legacy source: `wp-admin/includes/class-wp-upgrader.php`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-16** — Enforce BR-UPD-05: The site enters maintenance mode for the duration of install_package().
  - Legacy source: `wp-admin/includes/class-wp-upgrader.php`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-17** — Enforce BR-UPD-06: Core updates use update-core.php, which carries per-version file manifests so obsolete files can be removed.
  - Legacy source: `wp-admin/includes/update-core.php`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-18** — Enforce BR-UPD-07: Schema migrations are version-gated upgrade_NNN() functions applied in sequence from the site's current db version.
  - Legacy source: `wp-admin/includes/upgrade.php`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-19** — Enforce BR-UPD-08: dbDelta() diffs the declared CREATE TABLE schema against the live schema and emits the necessary ALTER statements.
  - Legacy source: `wp-admin/includes/upgrade.php`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-20** — Enforce BR-UPD-09: $wp_db_version in version.php is the single schema version marker.
  - Legacy source: `wp-includes/version.php`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

## Test Tasks

- [ ] **TT-01** — Happy-path test for the main flow (see `requirements.md` → Acceptance Criteria)
- [ ] **TT-02** — Failure-path test for the main error case
- [ ] **TT-03** — Assert BR-UPD-01 and its negative case
- [ ] **TT-04** — Assert BR-UPD-02 and its negative case
- [ ] **TT-05** — Assert BR-UPD-03 and its negative case
- [ ] **TT-06** — Assert BR-UPD-04 and its negative case
- [ ] **TT-07** — Assert BR-UPD-05 and its negative case
- [ ] **TT-08** — Assert BR-UPD-06 and its negative case
- [ ] **TT-09** — Assert BR-UPD-07 and its negative case
- [ ] **TT-10** — Assert BR-UPD-08 and its negative case
- [ ] **TT-11** — Assert every extension point in `design.md` can alter behaviour as documented

> ⚠️ **No legacy tests exist for this unit.** This checkout contains no test suite (`../inventory.md` §9, question Q1). Every test above must be written from the specification, not ported.

## Suggested Order

1. Algorithms first (T-01…) — they encode the behaviour the functions expose.
2. Public functions next, in dependency order within the unit.
3. Business-rule enforcement last, with a test per rule.
4. **Blocker:** this unit cannot be validated until `http-api`, `filesystem-api`, `options-and-transients`, `database-wpdb` exist.

> ⚠️ **This unit is inside the 23-module dependency cycle** (`../traceability/spec-impact-matrix.md` §1). It cannot be fully validated in isolation; plan an integration checkpoint with the other members of the cycle.

