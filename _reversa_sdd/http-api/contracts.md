# http-api — External Contract

> Generated by the **Writer** (Reversa) · doc_level `complete`
> The contract this unit exposes to or consumes from the outside world.
> Confidence: 🟢 CONFIRMED · 🟡 INFERRED · 🔴 GAP

---

## Contract summary

| Property | Value |
|----------|-------|
| Protocol / format | Outbound HTTP |
| Surface | `any external host` |
| Machine-readable spec | _none — this contract has no formal schema_ 🔴 |
| Direction | outbound |

The outbound contract: what WordPress will and will not request. 🟢

## Contract rules

These rules are **observable by a third party** and therefore part of the contract, not implementation detail. Breaking any of them is a breaking change.

| ID | Rule | Source | Confidence |
|----|------|--------|:----------:|
| `BR-HTTP-01` | Only wp_safe_remote_* validates the URL; wp_remote_* does not. | `wp-includes/http.php:81` | 🟢 |
| `BR-HTTP-02` | Only http and https schemes are accepted. | `wp-includes/http.php:564` | 🟢 |
| `BR-HTTP-03` | If wp_kses_bad_protocol() changes the URL in any way it is rejected rather than corrected: sanitization used as detection. | `wp-includes/http.php:565` | 🟢 |
| `BR-HTTP-04` | URLs containing userinfo (user:pass@host) are rejected outright. | `wp-includes/http.php:573` | 🟢 |
| `BR-HTTP-05` | Hosts containing : # ? [ or ] are rejected, which excludes IPv6 literal addresses entirely. | `wp-includes/http.php:576` | 🟢 |
| `BR-HTTP-06` | Requests to the site's own host bypass all IP-range checks so loopback cron and REST self-requests work. | `wp-includes/http.php:580` | 🟢 |
| `BR-HTTP-07` | A hostname whose gethostbyname() returns the input unchanged (resolution failure) is rejected. | `wp-includes/http.php:588` | 🟢 |
| `BR-HTTP-08` | 13 IPv4 ranges are blocked, including 169.254.0.0/16 which the inline comment names as cloud metadata. | `wp-includes/http.php:594` | 🟢 |
| `BR-HTTP-09` | A blocked host can be permitted via the http_request_host_is_external filter, which defaults to false. | `wp-includes/http.php:610` | 🟢 |
| `BR-HTTP-10` | With an explicit port only 80, 443 and 8080 are allowed, filterable via http_allowed_safe_ports. | `wp-includes/http.php:620` | 🟢 |
| `BR-HTTP-11` | A URL with no explicit port passes the port check unconditionally. | `wp-includes/http.php:616` | 🟢 |
| `BR-HTTP-12` | Transport is chosen per request between cURL and streams based on capability requirements. | `wp-includes/class-wp-http.php:547` | 🟢 |
| `BR-HTTP-13` | WP_HTTP_BLOCK_EXTERNAL with WP_ACCESSIBLE_HOSTS provides a site-wide egress allowlist. | `wp-includes/class-wp-http.php:895` | 🟢 |

## Entry points

| Symbol | Signature | Returns | Source |
|--------|-----------|---------|--------|
| `wp_http_validate_url` | `($url: string)` | `string|false` | `wp-includes/http.php:559` |
| `wp_safe_remote_get` | `($url, $args = array())` | `array|WP_Error` | `wp-includes/http.php:81` |
| `wp_remote_get` | `($url, $args = array())` | `array|WP_Error` | `wp-includes/http.php` |
| `WP_Http::request` | `($url, $args = array())` | `array|WP_Error` | `wp-includes/class-wp-http.php:169` |
| `WP_Http::_get_first_available_transport` | `($args, $url = null)` | `string|false` | `wp-includes/class-wp-http.php:547` |
| `WP_Http::_dispatch_request` | `($url, $args)` | `array|WP_Error` | `wp-includes/class-wp-http.php:598` |
| `WP_Http::block_request` | `($uri)` | `bool` | `wp-includes/class-wp-http.php:895` |

## Contract modifiers

Any of these hooks can change what this contract emits or accepts. **A consumer cannot rely on the contract without knowing the site's plugin set** (`../domain.md` DR-01). 🟢

- `http_request_host_is_external`
- `http_allowed_safe_ports`
- `http_request_args`
- `pre_http_request`
- `http_response`
- `http_api_curl`
- `http_request_timeout`
- `http_request_redirection_count`
- `block_local_requests`

## Compatibility commitments

| Commitment | Basis |
|------------|-------|
| Nothing in this contract is ever removed | ADR-002 |
| Deprecated elements keep working and warn only under `WP_DEBUG` | BR-DEP-01/02 |

## Risks

- 🟢 **F-HTTP-01** — The most carefully written security function in WordPress: the range list is annotated with RFC intent and the 169.254.0.0/16 comment names cloud metadata explicitly, evidence of threat-model-driven design.
- 🟢 **F-HTTP-02** — The sanitize-then-compare pattern (reject if changed rather than use the cleaned value) is the correct way to use a sanitizer as a validator and is rare in the codebase.
- 🟡 **F-HTTP-03** — Validation is by resolved IP but the request is made by hostname; a DNS entry resolving differently between check and request (DNS rebinding) defeats the guard, and nothing pins the resolved address.
- 🟡 **F-HTTP-04** — Only IPv4 is range-checked. IPv6 literals are rejected outright, but a hostname resolving to IPv6 is not checked because gethostbyname() returns IPv4 only.
- 🟢 **F-HTTP-05** — Safety is opt-in by function name, and the API makes the unsafe call the shorter and more obvious one.
- 🟡 **F-HTTP-06** — The same-host exemption means a site whose home option points at an internal hostname can be used to reach that host.

