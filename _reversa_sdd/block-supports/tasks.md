# block-supports — Implementation Tasks

> Generated by the **Writer** (Reversa) · doc_level `complete`
> A sequence of executable tasks to reimplement this unit from the legacy system.
> Every task cites the legacy file the behaviour was extracted from.

---

## Prerequisites

- [ ] Unit `block-editor` is available (see `design.md` → Dependencies)
- [ ] Unit `style-engine` is available (see `design.md` → Dependencies)
- [ ] Unit `global-styles-theme-json` is available (see `design.md` → Dependencies)
- [ ] Unit `hooks-plugin-api` is available (see `design.md` → Dependencies)
- [ ] Database schema matches `../data-dictionary.md` for: `WP_Block_Supports`
- [ ] PHP 7.4+ with the extensions listed in `../dependencies.md` §2

## Tasks

- [ ] **T-01** — Implement the *space-concatenation attribute merge* algorithm.
  - Behaviour: Each support's apply callback returns attributes; scalars (excluding booleans) are appended space-separated to any existing value for the same attribute name, letting 23 modules compose without coordination.
  - Legacy source: `wp-includes/class-wp-block-supports.php:114`
  - Definition of done: the algorithm reproduces the cited behaviour exactly, including its edge conditions
  - Confidence: 🟢

- [ ] **T-02** — Implement `WP_Block_Supports::register($block_support_name, $block_support_config)` returning `void`.
  - Legacy source: `wp-includes/class-wp-block-supports.php:99`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-03** — Implement `WP_Block_Supports::apply_block_supports()` returning `array`.
  - Legacy source: `wp-includes/class-wp-block-supports.php:114`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-04** — Implement `WP_Block_Supports::init()` returning `void`.
  - Legacy source: `wp-includes/class-wp-block-supports.php:77`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-05** — Implement `WP_Block_Supports::get_instance()` returning `WP_Block_Supports`.
  - Legacy source: `wp-includes/class-wp-block-supports.php:62`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-06** — Enforce BR-BSUP-01: Supports apply only to blocks with a registered block type; unregistered blocks receive nothing.
  - Legacy source: `wp-includes/class-wp-block-supports.php:120`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-07** — Enforce BR-BSUP-02: Attributes contributed by multiple supports are space-concatenated, with the first writer taking the slot and later ones appending.
  - Legacy source: `wp-includes/class-wp-block-supports.php:148`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-08** — Enforce BR-BSUP-03: Non-scalar values and booleans are skipped entirely; the explicit is_bool() check prevents true stringifying to '1' in markup.
  - Legacy source: `wp-includes/class-wp-block-supports.php:143`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-09** — Enforce BR-BSUP-04: A support without an apply callback contributes nothing at render time, though it may still register attributes.
  - Legacy source: `wp-includes/class-wp-block-supports.php:131`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-10** — Enforce BR-BSUP-05: Attributes pass through prepare_attributes_for_render() so schema defaults are applied before any support sees them.
  - Legacy source: `wp-includes/class-wp-block-supports.php:126`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-11** — Enforce BR-BSUP-06: The block currently being rendered is tracked in the public static $block_to_render.
  - Legacy source: `wp-includes/class-wp-block-supports.php:43`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

## Test Tasks

- [ ] **TT-01** — Happy-path test for the main flow (see `requirements.md` → Acceptance Criteria)
- [ ] **TT-02** — Failure-path test for the main error case
- [ ] **TT-03** — Assert BR-BSUP-01 and its negative case
- [ ] **TT-04** — Assert BR-BSUP-02 and its negative case
- [ ] **TT-05** — Assert BR-BSUP-03 and its negative case
- [ ] **TT-06** — Assert BR-BSUP-04 and its negative case
- [ ] **TT-07** — Assert BR-BSUP-05 and its negative case
- [ ] **TT-08** — Assert BR-BSUP-06 and its negative case
- [ ] **TT-09** — Assert every extension point in `design.md` can alter behaviour as documented

> ⚠️ **No legacy tests exist for this unit.** This checkout contains no test suite (`../inventory.md` §9, question Q1). Every test above must be written from the specification, not ported.

## Data Migration Tasks

- [ ] **TM-01** — Provision `WP_Block_Supports` per `../data-dictionary.md`
  - ⚠️ No foreign keys exist in the legacy schema; referential integrity is caller-enforced (`F-DD-01`)

## Suggested Order

1. Algorithms first (T-01…) — they encode the behaviour the functions expose.
2. Public functions next, in dependency order within the unit.
3. Business-rule enforcement last, with a test per rule.
4. **Blocker:** this unit cannot be validated until `block-editor`, `style-engine`, `global-styles-theme-json`, `hooks-plugin-api` exist.

> ⚠️ **This unit is inside the 23-module dependency cycle** (`../traceability/spec-impact-matrix.md` §1). It cannot be fully validated in isolation; plan an integration checkpoint with the other members of the cycle.

