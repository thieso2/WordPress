# sitemaps

> Generated by the **Writer** (Reversa) · unit granularity `module` · doc_level `complete`
> Source: WordPress `7.2-alpha-63330` · extracted by the Archaeologist from the files listed below.
> Confidence on every statement: 🟢 CONFIRMED (read from code) · 🟡 INFERRED · 🔴 GAP

---

## Overview

Generate XML sitemaps for search engines from a small provider-based architecture. 🟢

**Complexity:** `low` · **Business rules extracted:** 5 · **Public functions catalogued:** 3 · **Extension points:** 8

**Depended on by 0 module(s):** _none — terminal module_

## Responsibilities

- **provider registry with paginated index** — An abstract provider yields URL lists; the registry holds providers; the index builds a sitemap-of-sitemaps paginated at 2000 URLs per file, well under the 50000 protocol limit 🟢

## Data owned

### `WP_Sitemaps_Provider` 🟢

| Field | Type | Required | Note |
|-------|------|:--------:|------|
| `name` | `string` | yes | — |
| `object_type` | `string` | yes | — |

## Business Rules

Every rule below was read from the source file cited. These are the contract.

| ID | Rule | Source | Confidence |
|----|------|--------|:----------:|
| `BR-SM-01` | Three built-in providers cover posts, taxonomies and users. | `wp-includes/sitemaps/providers/` | 🟢 |
| `BR-SM-02` | Each sitemap file holds at most 2000 URLs, filterable per object type via wp_sitemaps_max_urls. | `wp-includes/sitemaps.php:90` | 🟢 |
| `BR-SM-03` | An XSL stylesheet is served so the raw XML renders readably in a browser. | `wp-includes/sitemaps/class-wp-sitemaps-stylesheet.php` | 🟢 |
| `BR-SM-04` | Only public post types and public taxonomies are included. | `wp-includes/sitemaps/providers/class-wp-sitemaps-posts.php` | 🟢 |
| `BR-SM-05` | The user provider includes only authors with published posts. | `wp-includes/sitemaps/providers/class-wp-sitemaps-users.php` | 🟢 |

## Functional Requirements

| ID | Requirement | Priority | Acceptance criterion |
|----|-------------|----------|----------------------|
| FR-01 | Three built-in providers cover posts, taxonomies and users | Must | Verify at `wp-includes/sitemaps/providers/` |
| FR-02 | Each sitemap file holds at most 2000 URLs, filterable per object type via wp_sitemaps_max_urls | Must | Verify at `wp-includes/sitemaps.php:90` |
| FR-03 | An XSL stylesheet is served so the raw XML renders readably in a browser | Should | Verify at `wp-includes/sitemaps/class-wp-sitemaps-stylesheet.php` |
| FR-04 | Only public post types and public taxonomies are included | Should | Verify at `wp-includes/sitemaps/providers/class-wp-sitemaps-posts.php` |
| FR-05 | The user provider includes only authors with published posts | Should | Verify at `wp-includes/sitemaps/providers/class-wp-sitemaps-users.php` |

## Non-Functional Requirements

_No non-functional requirement could be inferred from code evidence for this unit._ 🔴

## Acceptance Criteria

```gherkin
# BR-SM-01
Given the sitemaps module is loaded
When the behaviour at wp-includes/sitemaps/providers/ is exercised
Then Three built-in providers cover posts, taxonomies and users

# BR-SM-02
Given the sitemaps module is loaded
When the behaviour at wp-includes/sitemaps.php:90 is exercised
Then Each sitemap file holds at most 2000 URLs, filterable per object type via wp_sitemaps_max_urls

# BR-SM-03
Given the sitemaps module is loaded
When the behaviour at wp-includes/sitemaps/class-wp-sitemaps-stylesheet.php is exercised
Then An XSL stylesheet is served so the raw XML renders readably in a browser

# BR-SM-04
Given the sitemaps module is loaded
When the behaviour at wp-includes/sitemaps/providers/class-wp-sitemaps-posts.php is exercised
Then Only public post types and public taxonomies are included

# BR-SM-05
Given the sitemaps module is loaded
When the behaviour at wp-includes/sitemaps/providers/class-wp-sitemaps-users.php is exercised
Then The user provider includes only authors with published posts

# Failure path
Given an input that violates the constraints above
When the operation is attempted
Then the module must fail in the manner its rules describe
And must not silently produce a partial result
```

## Priority (MoSCoW)

| Requirement | MoSCoW | Rationale |
|-------------|--------|-----------|
| Core responsibilities | Must | terminal module — no other unit depends on it |
| `BR-SM-03` | Should | Secondary rule; terminal module — no other unit depends on it |
| `BR-SM-04` | Should | Secondary rule; terminal module — no other unit depends on it |
| `BR-SM-05` | Should | Secondary rule; terminal module — no other unit depends on it |

> Priority inferred from dependent count (`traceability/spec-impact-matrix.md`) and position in the dependency chain.

## Code Traceability

| File | Function / Class | Coverage |
|------|------------------|----------|
| `wp-includes/sitemaps.php` | `wp_sitemaps_get_max_urls`, `wp_get_sitemap_providers`, `wp_register_sitemap_provider` | 🟢 |
| `wp-includes/sitemaps/class-wp-sitemaps.php` | _module file_ | 🟡 |
| `wp-includes/sitemaps/class-wp-sitemaps-index.php` | _module file_ | 🟡 |
| `wp-includes/sitemaps/class-wp-sitemaps-provider.php` | _module file_ | 🟡 |
| `wp-includes/sitemaps/class-wp-sitemaps-registry.php` | _module file_ | 🟡 |
| `wp-includes/sitemaps/class-wp-sitemaps-renderer.php` | _module file_ | 🟡 |
| `wp-includes/sitemaps/class-wp-sitemaps-stylesheet.php` | _module file_ | 🟡 |
| `wp-includes/sitemaps/providers/` | _module file_ | 🟡 |

## Open points

- 🟡 **F-SM-02** — The 2000-URL page size is 4% of the protocol maximum, a memory-safety choice producing many more files than necessary on large sites.

See `../questions.md` for questions raised across the whole extraction.

