# sitemaps — Implementation Tasks

> Generated by the **Writer** (Reversa) · doc_level `complete`
> A sequence of executable tasks to reimplement this unit from the legacy system.
> Every task cites the legacy file the behaviour was extracted from.

---

## Prerequisites

- [ ] Unit `posts-and-post-types` is available (see `design.md` → Dependencies)
- [ ] Unit `taxonomy-and-terms` is available (see `design.md` → Dependencies)
- [ ] Unit `users-roles-capabilities` is available (see `design.md` → Dependencies)
- [ ] Unit `rewrite-and-permalinks` is available (see `design.md` → Dependencies)
- [ ] Unit `query-and-loop` is available (see `design.md` → Dependencies)
- [ ] Database schema matches `../data-dictionary.md` for: `WP_Sitemaps_Provider`
- [ ] PHP 7.4+ with the extensions listed in `../dependencies.md` §2

## Tasks

- [ ] **T-01** — Implement the *provider registry with paginated index* algorithm.
  - Behaviour: An abstract provider yields URL lists; the registry holds providers; the index builds a sitemap-of-sitemaps paginated at 2000 URLs per file, well under the 50000 protocol limit.
  - Legacy source: `wp-includes/sitemaps/class-wp-sitemaps-index.php`
  - Definition of done: the algorithm reproduces the cited behaviour exactly, including its edge conditions
  - Confidence: 🟢

- [ ] **T-02** — Implement `wp_sitemaps_get_max_urls($object_type)` returning `int`.
  - Legacy source: `wp-includes/sitemaps.php:90`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-03** — Implement `wp_get_sitemap_providers()` returning `WP_Sitemaps_Provider[]`.
  - Legacy source: `wp-includes/sitemaps.php`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-04** — Implement `wp_register_sitemap_provider($name, $provider)` returning `bool`.
  - Legacy source: `wp-includes/sitemaps.php`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-05** — Enforce BR-SM-01: Three built-in providers cover posts, taxonomies and users.
  - Legacy source: `wp-includes/sitemaps/providers/`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-06** — Enforce BR-SM-02: Each sitemap file holds at most 2000 URLs, filterable per object type via wp_sitemaps_max_urls.
  - Legacy source: `wp-includes/sitemaps.php:90`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-07** — Enforce BR-SM-03: An XSL stylesheet is served so the raw XML renders readably in a browser.
  - Legacy source: `wp-includes/sitemaps/class-wp-sitemaps-stylesheet.php`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-08** — Enforce BR-SM-04: Only public post types and public taxonomies are included.
  - Legacy source: `wp-includes/sitemaps/providers/class-wp-sitemaps-posts.php`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-09** — Enforce BR-SM-05: The user provider includes only authors with published posts.
  - Legacy source: `wp-includes/sitemaps/providers/class-wp-sitemaps-users.php`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

## Test Tasks

- [ ] **TT-01** — Happy-path test for the main flow (see `requirements.md` → Acceptance Criteria)
- [ ] **TT-02** — Failure-path test for the main error case
- [ ] **TT-03** — Assert BR-SM-01 and its negative case
- [ ] **TT-04** — Assert BR-SM-02 and its negative case
- [ ] **TT-05** — Assert BR-SM-03 and its negative case
- [ ] **TT-06** — Assert BR-SM-04 and its negative case
- [ ] **TT-07** — Assert BR-SM-05 and its negative case
- [ ] **TT-08** — Assert every extension point in `design.md` can alter behaviour as documented

> ⚠️ **No legacy tests exist for this unit.** This checkout contains no test suite (`../inventory.md` §9, question Q1). Every test above must be written from the specification, not ported.

## Data Migration Tasks

- [ ] **TM-01** — Provision `WP_Sitemaps_Provider` per `../data-dictionary.md`
  - ⚠️ No foreign keys exist in the legacy schema; referential integrity is caller-enforced (`F-DD-01`)

## Suggested Order

1. Algorithms first (T-01…) — they encode the behaviour the functions expose.
2. Public functions next, in dependency order within the unit.
3. Business-rule enforcement last, with a test per rule.
4. **Blocker:** this unit cannot be validated until `posts-and-post-types`, `taxonomy-and-terms`, `users-roles-capabilities`, `rewrite-and-permalinks` exist.

