# script-modules-and-assets — Implementation Tasks

> Generated by the **Writer** (Reversa) · doc_level `complete`
> A sequence of executable tasks to reimplement this unit from the legacy system.
> Every task cites the legacy file the behaviour was extracted from.

---

## Prerequisites

- [ ] Unit `hooks-plugin-api` is available (see `design.md` → Dependencies)
- [ ] Unit `internationalization` is available (see `design.md` → Dependencies)
- [ ] Unit `themes-and-templates` is available (see `design.md` → Dependencies)
- [ ] Database schema matches `../data-dictionary.md` for: `WP_Dependencies`, `_WP_Dependency`, `WP_Script_Modules`
- [ ] PHP 7.4+ with the extensions listed in `../dependencies.md` §2

## Tasks

- [ ] **T-01** — Implement the *recursive topological dependency resolution* algorithm.
  - Behaviour: all_deps() walks handles, computes missing deps via array_diff against the registry, recurses into declared deps, and appends survivors to to_do. Failure aborts a branch during recursion but only skips one handle at the top level.
  - Legacy source: `wp-includes/class-wp-dependencies.php:193`
  - Definition of done: the algorithm reproduces the cited behaviour exactly, including its edge conditions
  - Confidence: 🟢

- [ ] **T-02** — Implement the *monotonic group promotion* algorithm.
  - Behaviour: set_group() writes only when the new group is strictly lower than the recorded one, so a handle can move footer->header but never header->footer.
  - Legacy source: `wp-includes/class-wp-dependencies.php:511`
  - Definition of done: the algorithm reproduces the cited behaviour exactly, including its edge conditions
  - Confidence: 🟢

- [ ] **T-03** — Implement the *dependents-derived fetch priority* algorithm.
  - Behaviour: get_highest_fetchpriority() consults dependents_map so a module used by a high-priority module inherits urgency.
  - Legacy source: `wp-includes/class-wp-script-modules.php:477`
  - Definition of done: the algorithm reproduces the cited behaviour exactly, including its edge conditions
  - Confidence: 🟢

- [ ] **T-04** — Implement the *once-per-handle warning* algorithm.
  - Behaviour: Missing-dependency notices are recorded in a private array so repeated resolutions do not spam the log.
  - Legacy source: `wp-includes/class-wp-dependencies.php:220`
  - Definition of done: the algorithm reproduces the cited behaviour exactly, including its edge conditions
  - Confidence: 🟢

- [ ] **T-05** — Implement `WP_Dependencies::all_deps($handles, $recursion = false, $group = false)` returning `bool`.
  - Legacy source: `wp-includes/class-wp-dependencies.php:193`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-06** — Implement `WP_Dependencies::set_group($handle, $recursion, $group)` returning `bool`.
  - Legacy source: `wp-includes/class-wp-dependencies.php:511`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-07** — Implement `WP_Dependencies::add($handle, $src, $deps = array(), $ver = false, $args = null)` returning `bool`.
  - Legacy source: `wp-includes/class-wp-dependencies.php:281`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-08** — Implement `WP_Dependencies::enqueue($handles)` returning `void`.
  - Legacy source: `wp-includes/class-wp-dependencies.php:378`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-09** — Implement `WP_Dependencies::do_items($handles = false, $group = false)` returning `string[]`.
  - Legacy source: `wp-includes/class-wp-dependencies.php:134`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-10** — Implement `WP_Dependencies::get_etag($load)` returning `string`.
  - Legacy source: `wp-includes/class-wp-dependencies.php:533`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-11** — Implement `WP_Script_Modules::register($id, $src, $deps = array(), $version = false, $args = array())` returning `void`.
  - Legacy source: `wp-includes/class-wp-script-modules.php:133`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-12** — Implement `WP_Script_Modules::print_import_map()` returning `void`.
  - Legacy source: `wp-includes/class-wp-script-modules.php:620`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-13** — Implement `WP_Script_Modules::print_script_module_preloads()` returning `void`.
  - Legacy source: `wp-includes/class-wp-script-modules.php:585`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-14** — Implement `WP_Script_Modules::get_highest_fetchpriority($ids: array)` returning `string`.
  - Legacy source: `wp-includes/class-wp-script-modules.php:477`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-15** — Implement `wp_default_scripts($scripts)` returning `void`.
  - Legacy source: `wp-includes/script-loader.php`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-16** — Enforce BR-ASSET-01: A handle with an unregistered dependency is dropped from output entirely.
  - Legacy source: `wp-includes/class-wp-dependencies.php:218`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-17** — Enforce BR-ASSET-02: The missing-dependency _doing_it_wrong() notice fires at most once per handle per request, tracked in a private array.
  - Legacy source: `wp-includes/class-wp-dependencies.php:220`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-18** — Enforce BR-ASSET-03: Inside recursion a resolution failure returns false and aborts that branch; at the top level it skips only that handle and continues.
  - Legacy source: `wp-includes/class-wp-dependencies.php:232`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-19** — Enforce BR-ASSET-04: set_group() only ever moves a handle to a lower group number: footer to header, never the reverse.
  - Legacy source: `wp-includes/class-wp-dependencies.php:511`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-20** — Enforce BR-ASSET-05: Group 0 is the header and group 1 the footer.
  - Legacy source: `wp-includes/class-wp-dependencies.php:88`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-21** — Enforce BR-ASSET-06: A handle already in done is never re-emitted.
  - Legacy source: `wp-includes/class-wp-dependencies.php:203`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-22** — Enforce BR-ASSET-07: A '?' suffix on a handle is stripped before resolution and re-applied to the emitted URL.
  - Legacy source: `wp-includes/class-wp-dependencies.php:199`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-23** — Enforce BR-ASSET-08: Script-module fetch priority is derived from a module's dependents via dependents_map, not from its own declaration.
  - Legacy source: `wp-includes/class-wp-script-modules.php:477`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-24** — Enforce BR-ASSET-09: Script modules emit a browser import map plus modulepreload links for static dependencies.
  - Legacy source: `wp-includes/class-wp-script-modules.php:620`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-25** — Enforce BR-ASSET-10: Both registries use the same register -> enqueue -> to_do -> done lifecycle.
  - Legacy source: `both classes`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

## Test Tasks

- [ ] **TT-01** — Happy-path test for the main flow (see `requirements.md` → Acceptance Criteria)
- [ ] **TT-02** — Failure-path test for the main error case
- [ ] **TT-03** — Assert BR-ASSET-01 and its negative case
- [ ] **TT-04** — Assert BR-ASSET-02 and its negative case
- [ ] **TT-05** — Assert BR-ASSET-03 and its negative case
- [ ] **TT-06** — Assert BR-ASSET-04 and its negative case
- [ ] **TT-07** — Assert BR-ASSET-05 and its negative case
- [ ] **TT-08** — Assert BR-ASSET-06 and its negative case
- [ ] **TT-09** — Assert BR-ASSET-07 and its negative case
- [ ] **TT-10** — Assert BR-ASSET-08 and its negative case
- [ ] **TT-11** — Assert every extension point in `design.md` can alter behaviour as documented

> ⚠️ **No legacy tests exist for this unit.** This checkout contains no test suite (`../inventory.md` §9, question Q1). Every test above must be written from the specification, not ported.

## Data Migration Tasks

- [ ] **TM-01** — Provision `WP_Dependencies` per `../data-dictionary.md`
  - ⚠️ No foreign keys exist in the legacy schema; referential integrity is caller-enforced (`F-DD-01`)
- [ ] **TM-02** — Provision `_WP_Dependency` per `../data-dictionary.md`
  - ⚠️ No foreign keys exist in the legacy schema; referential integrity is caller-enforced (`F-DD-01`)
- [ ] **TM-03** — Provision `WP_Script_Modules` per `../data-dictionary.md`
  - ⚠️ No foreign keys exist in the legacy schema; referential integrity is caller-enforced (`F-DD-01`)

## Suggested Order

1. Algorithms first (T-01…) — they encode the behaviour the functions expose.
2. Public functions next, in dependency order within the unit.
3. Business-rule enforcement last, with a test per rule.
4. **Blocker:** this unit cannot be validated until `hooks-plugin-api`, `internationalization`, `themes-and-templates` exist.

> ⚠️ **This unit is inside the 23-module dependency cycle** (`../traceability/spec-impact-matrix.md` §1). It cannot be fully validated in isolation; plan an integration checkpoint with the other members of the cycle.

