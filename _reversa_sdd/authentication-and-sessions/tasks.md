# authentication-and-sessions — Implementation Tasks

> Generated by the **Writer** (Reversa) · doc_level `complete`
> A sequence of executable tasks to reimplement this unit from the legacy system.
> Every task cites the legacy file the behaviour was extracted from.

---

## Prerequisites

- [ ] Unit `users-roles-capabilities` is available (see `design.md` → Dependencies)
- [ ] Unit `metadata` is available (see `design.md` → Dependencies)
- [ ] Unit `options-and-transients` is available (see `design.md` → Dependencies)
- [ ] Unit `hooks-plugin-api` is available (see `design.md` → Dependencies)
- [ ] Unit `http-api` is available (see `design.md` → Dependencies)
- [ ] Database schema matches `../data-dictionary.md` for: `auth cookie`, `session token (wp_usermeta key session_tokens)`
- [ ] PHP 7.4+ with the extensions listed in `../dependencies.md` §2

## Tasks

- [ ] **T-01** — Implement the *bcrypt pre-hashing* algorithm.
  - Behaviour: base64(hmac_sha384(password, 'wp-sha384')) fed to password_hash(), prefixed '$wp'. Preserves entropy beyond bcrypt's 72-byte limit with domain separation and no null bytes.
  - Legacy source: `wp-includes/pluggable.php:2771`
  - Definition of done: the algorithm reproduces the cited behaviour exactly, including its edge conditions
  - Confidence: 🟢

- [ ] **T-02** — Implement the *two-stage cookie HMAC* algorithm.
  - Behaviour: wp_hash(user_login|pass_frag|expiration|token, scheme) derives a per-user key; that key HMAC-SHA256s user_login|expiration|token. The password fragment is never transmitted but changing it invalidates all cookies.
  - Legacy source: `wp-includes/pluggable.php:951`
  - Definition of done: the algorithm reproduces the cited behaviour exactly, including its edge conditions
  - Confidence: 🟢

- [ ] **T-03** — Implement the *half-life nonce ticks* algorithm.
  - Behaviour: tick = ceil(time() / (nonce_life/2)); verification accepts tick and tick-1, returning 1 or 2 to signal age. Nonces are deterministic within a 12-hour window.
  - Legacy source: `wp-includes/pluggable.php:2444`
  - Definition of done: the algorithm reproduces the cited behaviour exactly, including its edge conditions
  - Confidence: 🟢

- [ ] **T-04** — Implement the *pluggable override* algorithm.
  - Behaviour: Every function wrapped in if (! function_exists()); pluggable.php loads after all plugins so any plugin can replace the entire security core.
  - Legacy source: `wp-includes/pluggable.php`
  - Definition of done: the algorithm reproduces the cited behaviour exactly, including its edge conditions
  - Confidence: 🟢

- [ ] **T-05** — Implement `wp_authenticate($username, $password)` returning `WP_User|WP_Error`.
  - Legacy source: `wp-includes/pluggable.php:684`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-06** — Implement `wp_hash_password($password (SensitiveParameter))` returning `string`.
  - Legacy source: `wp-includes/pluggable.php:2753`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-07** — Implement `wp_check_password($password, $hash, $user_id = '')` returning `bool`.
  - Legacy source: `wp-includes/pluggable.php:2843`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-08** — Implement `wp_password_needs_rehash($hash, $user_id = '')` returning `bool`.
  - Legacy source: `wp-includes/pluggable.php:2911`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-09** — Implement `wp_generate_auth_cookie($user_id, $expiration, $scheme = 'auth', $token = '')` returning `string`.
  - Legacy source: `wp-includes/pluggable.php:951`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-10** — Implement `wp_validate_auth_cookie($cookie = '', $scheme = '')` returning `int|false`.
  - Legacy source: `wp-includes/pluggable.php:782`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-11** — Implement `wp_parse_auth_cookie($cookie = '', $scheme = '')` returning `array|false`.
  - Legacy source: `wp-includes/pluggable.php:1013`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-12** — Implement `wp_set_auth_cookie($user_id, $remember = false, $secure = '', $token = '')` returning `void`.
  - Legacy source: `wp-includes/pluggable.php:1071`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-13** — Implement `wp_clear_auth_cookie()` returning `void`.
  - Legacy source: `wp-includes/pluggable.php:1208`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-14** — Implement `wp_nonce_tick($action = -1)` returning `float`.
  - Legacy source: `wp-includes/pluggable.php:2444`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-15** — Implement `wp_create_nonce($action = -1)` returning `string`.
  - Legacy source: `wp-includes/pluggable.php:2537`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-16** — Implement `wp_verify_nonce($nonce, $action = -1)` returning `int|false`.
  - Legacy source: `wp-includes/pluggable.php:2474`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-17** — Implement `wp_salt($scheme = 'auth')` returning `string`.
  - Legacy source: `wp-includes/pluggable.php:2585`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-18** — Implement `wp_hash($data, $scheme = 'auth')` returning `string`.
  - Legacy source: `wp-includes/pluggable.php:2719`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-19** — Implement `auth_redirect()` returning `void`.
  - Legacy source: `wp-includes/pluggable.php:1280`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-20** — Implement `check_admin_referer($action = -1, $query_arg = '_wpnonce')` returning `int|false`.
  - Legacy source: `wp-includes/pluggable.php:1374`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-21** — Implement `wp_validate_redirect($location, $fallback_url = '')` returning `string`.
  - Legacy source: `wp-includes/pluggable.php:1669`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-22** — Enforce BR-AUTH-01: 35 security functions in pluggable.php can be wholly replaced by any plugin defining them first, because pluggable.php loads after all plugins.
  - Legacy source: `wp-includes/pluggable.php`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-23** — Enforce BR-AUTH-02: Passwords longer than 4096 bytes hash to '*', which never verifies. A DoS guard expressed as a permanently failing hash.
  - Legacy source: `wp-includes/pluggable.php:2761`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-24** — Enforce BR-AUTH-03: bcrypt passwords are pre-hashed with HMAC-SHA-384 using key 'wp-sha384', base64-encoded, and prefixed with $wp to survive bcrypt's 72-byte truncation.
  - Legacy source: `wp-includes/pluggable.php:2771`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-25** — Enforce BR-AUTH-04: Three password hash formats coexist: $P$ (phpass), $2y$ (vanilla bcrypt), $wp$2y$ (WordPress pre-hashed bcrypt).
  - Legacy source: `wp-includes/pluggable.php:2843`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-26** — Enforce BR-AUTH-05: The auth cookie HMAC key includes a 4-character fragment of the password hash, so changing the password invalidates every outstanding cookie.
  - Legacy source: `wp-includes/pluggable.php:960`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-27** — Enforce BR-AUTH-06: The password fragment is substr(hash, 8, 4) for $P$ and $2y$ hashes and substr(hash, -4) otherwise.
  - Legacy source: `wp-includes/pluggable.php:961`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-28** — Enforce BR-AUTH-07: Cookie HMACs are compared with hash_equals() for constant-time comparison.
  - Legacy source: `wp-includes/pluggable.php:830`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-29** — Enforce BR-AUTH-08: AJAX and POST requests receive a one-hour cookie-expiry grace period.
  - Legacy source: `wp-includes/pluggable.php:806`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-30** — Enforce BR-AUTH-09: Cookie lifetime is 14 days with remember-me and 2 days without; in remember-me mode the browser cookie outlives the HMAC expiration by 12 hours.
  - Legacy source: `wp-includes/pluggable.php:1073`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-31** — Enforce BR-AUTH-10: The logged_in cookie is only marked secure when the auth cookie is secure AND the home URL uses HTTPS.
  - Legacy source: `wp-includes/pluggable.php:1085`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-32** — Enforce BR-AUTH-11: Cookie validation order is expiry, then user existence, then HMAC, then session token.
  - Legacy source: `wp-includes/pluggable.php:800`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-33** — Enforce BR-AUTH-12: Nonce lifetime is 24 hours (nonce_life filter), ticking every 12 hours; both the current and previous tick are accepted.
  - Legacy source: `wp-includes/pluggable.php:2444`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-34** — Enforce BR-AUTH-13: wp_verify_nonce() returns 1 (0-12h old), 2 (12-24h old) or false, never true.
  - Legacy source: `wp-includes/pluggable.php:2493`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-35** — Enforce BR-AUTH-14: A nonce is 10 hex characters taken from offset -12 of a wp_hash() result, roughly 40 bits.
  - Legacy source: `wp-includes/pluggable.php:2492`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-36** — Enforce BR-AUTH-15: Nonces bind to the session token, so logging out invalidates outstanding nonces.
  - Legacy source: `wp-includes/pluggable.php:2491`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-37** — Enforce BR-AUTH-16: Logged-out users all share uid 0 for nonce generation unless the nonce_user_logged_out filter supplies an identity.
  - Legacy source: `wp-includes/pluggable.php:2482`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

## Test Tasks

- [ ] **TT-01** — Happy-path test for the main flow (see `requirements.md` → Acceptance Criteria)
- [ ] **TT-02** — Failure-path test for the main error case
- [ ] **TT-03** — Assert BR-AUTH-01 and its negative case
- [ ] **TT-04** — Assert BR-AUTH-02 and its negative case
- [ ] **TT-05** — Assert BR-AUTH-03 and its negative case
- [ ] **TT-06** — Assert BR-AUTH-04 and its negative case
- [ ] **TT-07** — Assert BR-AUTH-05 and its negative case
- [ ] **TT-08** — Assert BR-AUTH-06 and its negative case
- [ ] **TT-09** — Assert BR-AUTH-07 and its negative case
- [ ] **TT-10** — Assert BR-AUTH-08 and its negative case
- [ ] **TT-11** — Assert every extension point in `design.md` can alter behaviour as documented

> ⚠️ **No legacy tests exist for this unit.** This checkout contains no test suite (`../inventory.md` §9, question Q1). Every test above must be written from the specification, not ported.

## Data Migration Tasks

- [ ] **TM-01** — Provision `auth cookie` per `../data-dictionary.md`
  - ⚠️ No foreign keys exist in the legacy schema; referential integrity is caller-enforced (`F-DD-01`)
- [ ] **TM-02** — Provision `session token (wp_usermeta key session_tokens)` per `../data-dictionary.md`
  - ⚠️ No foreign keys exist in the legacy schema; referential integrity is caller-enforced (`F-DD-01`)

## Suggested Order

1. Algorithms first (T-01…) — they encode the behaviour the functions expose.
2. Public functions next, in dependency order within the unit.
3. Business-rule enforcement last, with a test per rule.
4. **Blocker:** this unit cannot be validated until `users-roles-capabilities`, `metadata`, `options-and-transients`, `hooks-plugin-api` exist.

> ⚠️ **This unit is inside the 23-module dependency cycle** (`../traceability/spec-impact-matrix.md` §1). It cannot be fully validated in isolation; plan an integration checkpoint with the other members of the cycle.

