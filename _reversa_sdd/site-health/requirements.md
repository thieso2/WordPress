# site-health

> Generated by the **Writer** (Reversa) · unit granularity `module` · doc_level `complete`
> Source: WordPress `7.2-alpha-63330` · extracted by the Archaeologist from the files listed below.
> Confidence on every statement: 🟢 CONFIRMED (read from code) · 🟡 INFERRED · 🔴 GAP

---

## Overview

Run diagnostic tests and surface configuration problems, with a debug-data collector for support. 🟢

**Complexity:** `medium` · **Business rules extracted:** 5 · **Public functions catalogued:** 3 · **Extension points:** 4

**Depended on by 0 module(s):** _none — terminal module_

## Responsibilities

- **direct/async test split** — Tests are partitioned so that expensive checks (loopback HTTP, remote version checks) run asynchronously via JavaScript after page load 🟢

## Data owned

### `site health test result` 🟢

| Field | Type | Required | Note |
|-------|------|:--------:|------|
| `label` | `string` | yes | — |
| `status` | `'good'|'recommended'|'critical'` | yes | — |
| `badge` | `array{label,color}` | yes | — |
| `description` | `string` | yes | — |
| `actions` | `string` | no | — |
| `test` | `string` | yes | — |

## Business Rules

Every rule below was read from the source file cited. These are the contract.

| ID | Rule | Source | Confidence |
|----|------|--------|:----------:|
| `BR-SH-01` | Tests are either direct (run synchronously during page load) or async (fetched by JavaScript), so slow tests do not block the page. | `wp-admin/includes/class-wp-site-health.php` | 🟢 |
| `BR-SH-02` | Test statuses are good, recommended and critical. | `wp-admin/includes/class-wp-site-health.php` | 🟢 |
| `BR-SH-03` | WP_Site_Health is instantiated during boot specifically so its cron events can fire. | `wp-settings.php:760` | 🟢 |
| `BR-SH-04` | The loopback test is how a site detects that WP-Cron cannot self-invoke. | `wp-admin/includes/class-wp-site-health.php` | 🟢 |
| `BR-SH-05` | Results are also exposed through the site-health REST controller. | `wp-includes/rest-api/endpoints/class-wp-rest-site-health-controller.php` | 🟢 |

## Functional Requirements

| ID | Requirement | Priority | Acceptance criterion |
|----|-------------|----------|----------------------|
| FR-01 | Tests are either direct (run synchronously during page load) or async (fetched by JavaScript), so slow tests do not block the page | Must | Verify at `wp-admin/includes/class-wp-site-health.php` |
| FR-02 | Test statuses are good, recommended and critical | Must | Verify at `wp-admin/includes/class-wp-site-health.php` |
| FR-03 | WP_Site_Health is instantiated during boot specifically so its cron events can fire | Should | Verify at `wp-settings.php:760` |
| FR-04 | The loopback test is how a site detects that WP-Cron cannot self-invoke | Should | Verify at `wp-admin/includes/class-wp-site-health.php` |
| FR-05 | Results are also exposed through the site-health REST controller | Should | Verify at `wp-includes/rest-api/endpoints/class-wp-rest-site-health-controller.php` |

## Non-Functional Requirements

_No non-functional requirement could be inferred from code evidence for this unit._ 🔴

## Acceptance Criteria

```gherkin
# BR-SH-01
Given the site-health module is loaded
When the behaviour at wp-admin/includes/class-wp-site-health.php is exercised
Then Tests are either direct (run synchronously during page load) or async (fetched by JavaScript), so slow tests do not block the page

# BR-SH-02
Given the site-health module is loaded
When the behaviour at wp-admin/includes/class-wp-site-health.php is exercised
Then Test statuses are good, recommended and critical

# BR-SH-03
Given the site-health module is loaded
When the behaviour at wp-settings.php:760 is exercised
Then WP_Site_Health is instantiated during boot specifically so its cron events can fire

# BR-SH-04
Given the site-health module is loaded
When the behaviour at wp-admin/includes/class-wp-site-health.php is exercised
Then The loopback test is how a site detects that WP-Cron cannot self-invoke

# BR-SH-05
Given the site-health module is loaded
When the behaviour at wp-includes/rest-api/endpoints/class-wp-rest-site-health-controller.php is exercised
Then Results are also exposed through the site-health REST controller

# Failure path
Given an input that violates the constraints above
When the operation is attempted
Then the module must fail in the manner its rules describe
And must not silently produce a partial result
```

## Priority (MoSCoW)

| Requirement | MoSCoW | Rationale |
|-------------|--------|-----------|
| Core responsibilities | Must | terminal module — no other unit depends on it |
| `BR-SH-03` | Should | Secondary rule; terminal module — no other unit depends on it |
| `BR-SH-04` | Should | Secondary rule; terminal module — no other unit depends on it |
| `BR-SH-05` | Should | Secondary rule; terminal module — no other unit depends on it |

> Priority inferred from dependent count (`traceability/spec-impact-matrix.md`) and position in the dependency chain.

## Code Traceability

| File | Function / Class | Coverage |
|------|------------------|----------|
| `wp-admin/site-health.php` | _module file_ | 🟡 |
| `wp-admin/includes/class-wp-site-health.php` | `WP_Site_Health::get_tests`, `WP_Site_Health::get_test_loopback_requests` | 🟢 |
| `wp-admin/includes/class-wp-site-health-auto-updates.php` | _module file_ | 🟡 |
| `wp-admin/includes/class-wp-debug-data.php` | `WP_Debug_Data::debug_data` | 🟢 |
| `wp-includes/rest-api/endpoints/class-wp-rest-site-health-controller.php` | _module file_ | 🟡 |

## Open points

- 🟡 **F-SH-02** — Instantiating a diagnostic class on every request purely to keep a cron event registered is a small but real site-wide cost paid for an admin feature.

See `../questions.md` for questions raised across the whole extraction.

