# site-health — Implementation Tasks

> Generated by the **Writer** (Reversa) · doc_level `complete`
> A sequence of executable tasks to reimplement this unit from the legacy system.
> Every task cites the legacy file the behaviour was extracted from.

---

## Prerequisites

- [ ] Unit `http-api` is available (see `design.md` → Dependencies)
- [ ] Unit `cron` is available (see `design.md` → Dependencies)
- [ ] Unit `updates-and-upgrader` is available (see `design.md` → Dependencies)
- [ ] Unit `rest-api` is available (see `design.md` → Dependencies)
- [ ] Unit `admin-application` is available (see `design.md` → Dependencies)
- [ ] Unit `database-wpdb` is available (see `design.md` → Dependencies)
- [ ] Database schema matches `../data-dictionary.md` for: `site health test result`
- [ ] PHP 7.4+ with the extensions listed in `../dependencies.md` §2

## Tasks

- [ ] **T-01** — Implement the *direct/async test split* algorithm.
  - Behaviour: Tests are partitioned so that expensive checks (loopback HTTP, remote version checks) run asynchronously via JavaScript after page load.
  - Legacy source: `wp-admin/includes/class-wp-site-health.php`
  - Definition of done: the algorithm reproduces the cited behaviour exactly, including its edge conditions
  - Confidence: 🟢

- [ ] **T-02** — Implement `WP_Site_Health::get_tests()` returning `array`.
  - Legacy source: `wp-admin/includes/class-wp-site-health.php`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-03** — Implement `WP_Site_Health::get_test_loopback_requests()` returning `array`.
  - Legacy source: `wp-admin/includes/class-wp-site-health.php`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-04** — Implement `WP_Debug_Data::debug_data()` returning `array`.
  - Legacy source: `wp-admin/includes/class-wp-debug-data.php`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-05** — Enforce BR-SH-01: Tests are either direct (run synchronously during page load) or async (fetched by JavaScript), so slow tests do not block the page.
  - Legacy source: `wp-admin/includes/class-wp-site-health.php`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-06** — Enforce BR-SH-02: Test statuses are good, recommended and critical.
  - Legacy source: `wp-admin/includes/class-wp-site-health.php`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-07** — Enforce BR-SH-03: WP_Site_Health is instantiated during boot specifically so its cron events can fire.
  - Legacy source: `wp-settings.php:760`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-08** — Enforce BR-SH-04: The loopback test is how a site detects that WP-Cron cannot self-invoke.
  - Legacy source: `wp-admin/includes/class-wp-site-health.php`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-09** — Enforce BR-SH-05: Results are also exposed through the site-health REST controller.
  - Legacy source: `wp-includes/rest-api/endpoints/class-wp-rest-site-health-controller.php`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

## Test Tasks

- [ ] **TT-01** — Happy-path test for the main flow (see `requirements.md` → Acceptance Criteria)
- [ ] **TT-02** — Failure-path test for the main error case
- [ ] **TT-03** — Assert BR-SH-01 and its negative case
- [ ] **TT-04** — Assert BR-SH-02 and its negative case
- [ ] **TT-05** — Assert BR-SH-03 and its negative case
- [ ] **TT-06** — Assert BR-SH-04 and its negative case
- [ ] **TT-07** — Assert BR-SH-05 and its negative case
- [ ] **TT-08** — Assert every extension point in `design.md` can alter behaviour as documented

> ⚠️ **No legacy tests exist for this unit.** This checkout contains no test suite (`../inventory.md` §9, question Q1). Every test above must be written from the specification, not ported.

## Data Migration Tasks

- [ ] **TM-01** — Provision `site health test result` per `../data-dictionary.md`
  - ⚠️ No foreign keys exist in the legacy schema; referential integrity is caller-enforced (`F-DD-01`)

## Suggested Order

1. Algorithms first (T-01…) — they encode the behaviour the functions expose.
2. Public functions next, in dependency order within the unit.
3. Business-rule enforcement last, with a test per rule.
4. **Blocker:** this unit cannot be validated until `http-api`, `cron`, `updates-and-upgrader`, `rest-api` exist.

