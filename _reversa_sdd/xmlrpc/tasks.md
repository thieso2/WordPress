# xmlrpc — Implementation Tasks

> Generated by the **Writer** (Reversa) · doc_level `complete`
> A sequence of executable tasks to reimplement this unit from the legacy system.
> Every task cites the legacy file the behaviour was extracted from.

---

## Prerequisites

- [ ] Unit `authentication-and-sessions` is available (see `design.md` → Dependencies)
- [ ] Unit `users-roles-capabilities` is available (see `design.md` → Dependencies)
- [ ] Unit `posts-and-post-types` is available (see `design.md` → Dependencies)
- [ ] Unit `comments` is available (see `design.md` → Dependencies)
- [ ] Unit `media-and-attachments` is available (see `design.md` → Dependencies)
- [ ] Unit `http-api` is available (see `design.md` → Dependencies)
- [ ] PHP 7.4+ with the extensions listed in `../dependencies.md` §2

## Tasks

- [ ] **T-01** — Implement the *layered enablement resolution* algorithm.
  - Behaviour: pre_option_enable_xmlrpc, then option_enable_xmlrpc defaulting to true, then the xmlrpc_enabled filter; the chain exists to respect back-compat filters written against the removed option.
  - Legacy source: `wp-includes/class-wp-xmlrpc-server.php:185`
  - Definition of done: the algorithm reproduces the cited behaviour exactly, including its edge conditions
  - Confidence: 🟢

- [ ] **T-02** — Implement the *per-call credential authentication* algorithm.
  - Behaviour: Each method receives username and password as parameters and authenticates independently, so credentials traverse the wire on every request with no built-in rate limit.
  - Legacy source: `wp-includes/class-wp-xmlrpc-server.php`
  - Definition of done: the algorithm reproduces the cited behaviour exactly, including its edge conditions
  - Confidence: 🟢

- [ ] **T-03** — Implement `wp_xmlrpc_server::set_is_enabled()` returning `void`.
  - Legacy source: `wp-includes/class-wp-xmlrpc-server.php:185`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-04** — Implement `wp_xmlrpc_server::login($username, $password)` returning `WP_User|false`.
  - Legacy source: `wp-includes/class-wp-xmlrpc-server.php`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-05** — Implement `wp_xmlrpc_server::pingback_ping($args)` returning `string|IXR_Error`.
  - Legacy source: `wp-includes/class-wp-xmlrpc-server.php`
  - Definition of done: signature and return contract match; all business rules citing this file are satisfied
  - Confidence: 🟢

- [ ] **T-06** — Enforce BR-XR-01: XML-RPC is enabled by default; disabling requires add_filter('xmlrpc_enabled', '__return_false').
  - Legacy source: `wp-includes/class-wp-xmlrpc-server.php:195`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-07** — Enforce BR-XR-02: The enable_xmlrpc option is deprecated since 3.5.0; its pre_option and option filters are consulted only for back-compat.
  - Legacy source: `wp-includes/class-wp-xmlrpc-server.php:191`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-08** — Enforce BR-XR-03: Every method authenticates independently with a username and password passed as XML-RPC parameters; there is no session, token or nonce.
  - Legacy source: `wp-includes/class-wp-xmlrpc-server.php`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-09** — Enforce BR-XR-04: 78 methods span the wp, metaWeblog, mt, blogger, pingback and demo families.
  - Legacy source: `wp-includes/class-wp-xmlrpc-server.php`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

- [ ] **T-10** — Enforce BR-XR-05: pingback.ping fetches a caller-supplied URL server-side, unauthenticated.
  - Legacy source: `wp-includes/class-wp-xmlrpc-server.php`
  - Definition of done: a test asserts this rule holds, and asserts the negative case
  - Confidence: 🟢

## Test Tasks

- [ ] **TT-01** — Happy-path test for the main flow (see `requirements.md` → Acceptance Criteria)
- [ ] **TT-02** — Failure-path test for the main error case
- [ ] **TT-03** — Assert BR-XR-01 and its negative case
- [ ] **TT-04** — Assert BR-XR-02 and its negative case
- [ ] **TT-05** — Assert BR-XR-03 and its negative case
- [ ] **TT-06** — Assert BR-XR-04 and its negative case
- [ ] **TT-07** — Assert BR-XR-05 and its negative case
- [ ] **TT-08** — Assert every extension point in `design.md` can alter behaviour as documented

> ⚠️ **No legacy tests exist for this unit.** This checkout contains no test suite (`../inventory.md` §9, question Q1). Every test above must be written from the specification, not ported.

## Suggested Order

1. Algorithms first (T-01…) — they encode the behaviour the functions expose.
2. Public functions next, in dependency order within the unit.
3. Business-rule enforcement last, with a test per rule.
4. **Blocker:** this unit cannot be validated until `authentication-and-sessions`, `users-roles-capabilities`, `posts-and-post-types`, `comments` exist.

