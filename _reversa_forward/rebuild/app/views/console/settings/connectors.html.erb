<%# console.options-connectors — options-connectors.php. AI provider configuration,
    the `Assistance` context (target_screens.md:542), scheduled for Wave 5. The screen
    exists and is honest about its state: it persists nothing yet.
    deferred: assistance-connectors (Wave 5) -%>
<% @page_title = "Connectors" %>
<h1 class="wp-heading-inline">Connectors</h1>
<hr class="wp-header-end">
<%= render "console/settings/nav" %>

<div class="notice" role="status">
  <p><strong>Connectors are part of Wave 5.</strong> AI provider configuration binds to the
  <code>Assistance</code> context, which is not built in this pass. This screen is a truthful
  placeholder: the route and the navigation exist, and no provider setting is stored yet.</p>
</div>
