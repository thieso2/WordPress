<%# console.ms-themes — network/themes.php. P-LIST over the installed themes; the whole
    screen is the Console::ListModel the controller built. -%>
<%= render "console/network/nav" %>
<%= render "console/shared/list", list: @list %>
