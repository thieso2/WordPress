<%# console.ms-sites — network/sites.php. A P-LIST instantiation: the whole screen is the
    Console::ListModel the controller built, rendered by the ONE shared partial. Nothing
    here asks Access (BR-CAP-05) and nothing here invents copy (DEV-009). -%>
<%= render "console/network/nav" %>
<%= render "console/shared/list", list: @list %>
