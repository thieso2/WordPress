# Schema — .reversa/context/surface.json

File generated by the Scout. Used by the other agents as a source of structured context.

## Full structure

```json
{
  "generated_at": "2026-04-26T10:00:00Z",
  "project_root": "/path/to/project",
  "languages": [
    { "name": "TypeScript", "extensions": [".ts", ".tsx"], "file_count": 142 },
    { "name": "JavaScript", "extensions": [".js", ".mjs"], "file_count": 23 }
  ],
  "primary_language": "TypeScript",
  "frameworks": [
    { "name": "Next.js", "version": "14.2.0", "source": "package.json" },
    { "name": "Prisma", "version": "5.10.0", "source": "package.json" }
  ],
  "package_manager": "npm",
  "entry_points": [
    { "path": "src/app/layout.tsx", "type": "app_entry" },
    { "path": "src/server.ts", "type": "server_entry" }
  ],
  "config_files": [
    "next.config.js", ".env.example", "tsconfig.json"
  ],
  "ci_cd": [
    ".github/workflows/deploy.yml"
  ],
  "docker": {
    "dockerfile": "Dockerfile",
    "compose": "docker-compose.yml"
  },
  "database_hints": [
    { "path": "prisma/schema.prisma", "type": "prisma_schema" },
    { "path": "prisma/migrations/", "type": "migrations_dir" }
  ],
  "test_framework": "Jest",
  "test_file_count": 47,
  "modules": [
    "auth", "orders", "payments", "users", "notifications"
  ],
  "total_files": 312,
  "organization_suggestion": {
    "granularity": "module",
    "rationale": "The top-level folder structure is organized by domain: auth/, orders/, payments/, users/, notifications/.",
    "signals": [
      { "type": "top_level_domain_folders", "evidence": ["src/auth/", "src/orders/", "src/payments/"] }
    ],
    "features": []
  }
}
```

## Required fields

`generated_at`, `languages`, `primary_language`, `frameworks`, `entry_points`, `modules`, `organization_suggestion`

## Optional fields

All the others; include only what you find.

## The `organization_suggestion` field

A suggestion for how to organize this project's specs. Read by the Reversa orchestrator to pre-select the default option in the spec organization menu.

### Subfields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `granularity` | string | yes | One of: `module`, `use-case`, `endpoint`, `hybrid`, `feature`, `custom`. The Scout never suggests `custom`; that value only comes from the user's choice. |
| `rationale` | string | yes | A short sentence explaining why this granularity was chosen. It appears in the menu as the Scout's "Reason:". |
| `signals` | array | yes | List of detected signals that led to the suggestion. Each item has `type` and `evidence` (a list of paths). It may be empty when the `feature` fallback is used. |
| `features` | array | yes when `granularity = "feature"` | List of feature names discovered by the Scout. Each name becomes a top-level folder. |

### Heuristics for setting `granularity`

| Detected signal | Suggested `granularity` |
|-----------------|-------------------------|
| Centralized routing (`routes.*`, `urls.py`, `*Controller.cs`, `@RestController`) | `endpoint` |
| Top-level folders with domain names (`auth/`, `orders/`, `payments/`) | `module` |
| Behavior-oriented Gherkin / E2E specs (`features/*.feature`, BDD `*.spec.*`) | `use-case` |
| Several signals coexisting with similar weight | `hybrid` |
| No clear organizational signal | `feature` (fallback, fill `features` with whatever could be extracted) |

### Immutability

After the first run, the orchestrator persists the suggested `granularity` in `.reversa/config.toml` under the `scout_suggestion` field. On re-runs, the Scout may regenerate `surface.json` (the legacy system may have changed), but the orchestrator does NOT update `scout_suggestion` in `config.toml` (FR-14 of the spec organization spec).

## Note

Use this schema as a guide. If a field does not apply to the project, omit it — except for the required ones listed above.
