# Template — _reversa_sdd/questions.md

This file is generated by the Reviewer and filled in by the user.

---

## How the Reviewer creates this file

For each 🔴 gap that only the user can resolve, create an entry:

```markdown
## Question [N]

**Context:** [Where in the system this doubt arose — module, function, line]
**Affected spec:** [`_reversa_sdd/<unit>/{requirements|design|tasks}.md`]
**Question:** [Direct, unambiguous question]
**Impact:** [What changes in the spec depending on the answer]

**Answer:** <!-- fill in here -->
```

---

## How the user answers

The user should:
1. Fill in the `**Answer:**` field of each question
2. Tell the Reviewer when they are done (type `reversa` or "I answered the questions")

---

## How the Reviewer processes the answers

After being notified by the user:
1. Read the whole file
2. For each question with an answer filled in:
   - Update the corresponding spec
   - Reclassify the item according to the answer (🔴→🟢 or 🔴→🟡)
   - Mark the question as `✅ Answered` above the Answer field
3. For unanswered questions: keep them as 🔴 in the final report
4. Update `confidence-report.md` with the new percentages

---

## Example of a generated file

```markdown
# Validation Questions — [Project Name]

> Generated by the Reviewer on [date]
> Answer each question and let me know when you're done.

---

## Question 1

**Context:** Module `auth` — function `validateSession()` in `src/auth/session.ts:47`
**Affected spec:** [`_reversa_sdd/auth/requirements.md`]
**Question:** Does the session expire on inactivity or only after an absolute time? The code uses `lastActivity` but there is no visible expiration logic.
**Impact:** If it is inactivity-based, the spec must include the maximum idle time and the behavior when the session is renewed.

**Answer:** <!-- fill in here -->

---

## Question 2

**Context:** Module `orders` — constant `MAX_ITEMS_PER_ORDER = 50` in `src/orders/constants.ts:12`
**Affected spec:** [`_reversa_sdd/orders/requirements.md`]
**Question:** Is the 50-items-per-order limit a business rule or a temporary technical limit? Are there plans to raise it?
**Impact:** If it is a business rule, it must appear as 🟢 in the constraints. If it is technical, it should be 🟡 with a note for future review.

**Answer:** <!-- fill in here -->
```
